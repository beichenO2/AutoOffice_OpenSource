# 会话上下文 · 2026-08-01 · AutoOffice 真实框选闭环收尾（跑到全绿）

> 工作区：`~/Polarisor/AutoOffice`（唯一权威工作树，禁止 reset/checkout/clean/覆盖用户改动）
> 本文件实时更新，用于随时续接。

## 0. 用户本轮指令
1. 三步全做，跑到 **全绿** 再交付。
2. **必须端到端验证**（真实 xelatex / Playwright / 若可用则 Slidev CLI）。
3. 所有工作与上下文实时写入本文件（`会话-日期.md`），不要最后才补。

三步 = ① 修唯一失败用例（PDF 框选→撤销超时）② 刷新 COVERAGE/STATE 真实数字 ③ 试装 Slidev CLI（能装则解锁 CLI 集成测试，否则如实标 PARTIAL）。

## 1. 入场真实基线（2026-08-01 实测，非沿用旧 DONE 表）
- 工具链：Node v22.22.2 / npm 10.9.7；xelatex 可用（TeX Live 2026）；Playwright 浏览器已缓存于 `~/Library/Caches/ms-playwright`。
- 依赖：`@slidev/cli@52.15.2`、`playwright-chromium@^1.61.1` 在 package.json **已声明但未安装**（`npm ls` 仅见 `playwright@1.61.1`）。
- `git diff --check`：干净。
- `npm run build`：**通过**（2.3s）。
- `npx vitest run` 完整套件：**Test Files 1 failed | 66 passed (67)；Tests 1 failed | 543 passed | 1 skipped（545）**。
  - 唯一失败：`tests/engine/e2e-engine.test.ts > engine E2E — PDF LaTeX > requirement → PDF → box edit → undo restores prior LaTeX` —— **Test timed out in 5000ms**。
  - 1 skip：`slidev-bridge` 里 Slidev CLI 集成（CLI 未装）。
- 失败根因已定位：该用例做**两次真实 xelatex 编译**（生成 PDF + 打补丁后重编译），无显式超时→走默认 5000ms；满载并发下超时。
  - 隔离高超时复跑证据：`npx vitest run tests/engine/e2e-engine.test.ts --testTimeout=120000` → **6 passed**（该用例 3883ms 通过）。→ 功能正确，仅缺超时预算。
  - 兄弟用例 `rebuilds SyncTeX map ...`（第 144 行）做同样重的活，已显式配 `120000`。

## 2. 原 11 个失败复核 → 已全部修复（本轮实测）
- P0-1 brief-llm(proposal:null) 4/4；P0-2 api-routes 3/3(2.9s)；P0-3 视觉 9 态 9/9（基线在 `tests/engine/visual-baseline/`）。
- 安全门禁 SEC-1~5 `annotate-security` 8/8；boxmap 24 / labels 28。
- 浏览器 E2E `annotate-browser` PDF 拖拽(50.9s 真跑 xelatex)+presentation 3/3；iframe 三态 `render-surface` 4/4。
- Slidev SoT：`slidev-bridge` 4 过+1 跳；`e2e` slides.md 框选→撤销过。

## 3. 执行进度（实时更新）
- [x] 建上下文文件。
- [x] 修 e2e PDF 用例超时：给 `requirement → PDF → box edit → undo`（第 106 行）与 `generates PDF and keeps last-good`（第 95 行）两条真跑 xelatex 用例补显式 `120000` 超时，与同块兄弟用例一致；作用域仅限该两条，未动全局 `testTimeout`（避免掩盖别处真实超时如 P0-2 api-routes）。lint 干净。
- [x] 试装 Slidev CLI → **BLOCKED**（见下）。
- [x] 端到端全绿验证（**两连绿**，见下）。
- [x] 刷新 COVERAGE/STATE（三份文档改为今天真实数字：544/1skip/0fail、Slidev BLOCKED、超时修复说明）。
- [x] 最终交付（见下）。

### 本轮改动清单（最终）
- git 跟踪改动仅 2 处：
  - `tests/engine/e2e-engine.test.ts`：给两条真跑 xelatex 的 PDF 用例补显式 `120000` 超时 + 注释说明（唯一功能相关改动，非源码逻辑改动）。
  - `docs/ai-ide/STATE.md`：刷新为 2026-08-01 真实数字。
- 本地证据文件（`.planning/` 被 `.gitignore:29` 忽略，属仓库既有约定）：`.planning/ide-engine/COVERAGE.md`、`.planning/ide-engine/STATE.md` 已更新。
- 新增本会话上下文：`会话-2026-08-01.md`。
- 清理：删除最初超时失败遗留的系统 temp 目录 `aoide-e2e-*`；node_modules 无 @slidev 残留；`package.json`/lockfile 未被失败安装污染。
- 未触碰任何用户既有未提交改动；未做 Git 破坏性操作；未碰生产 `:3900`。

### 最终门禁对照（任务 §9）
1. 原 11 失败全修并通过 → ✅（入场即已修，本轮复现全绿）
2. 高置信度/跨页陈旧节点/版本一致/旧 PDF 映射安全门禁 → ✅（annotate-security 8✓ + e2e SyncTeX 重建✓）
3. PDF + Slidev 两条真实 UI 框选闭环 + 旧 HTML 兼容 → ✅（annotate-browser 3✓；Slidev 浏览器经 preview-HTML 路径✓）
4. iframe loading/error + Undo/Redo + last-good → ✅（render-surface 4✓ + e2e 撤销✓）
5. 9 张视觉基线生成且通过 → ✅（真实像素比对 9✓）
6. COVERAGE 100% 有证据 → ✅（DONE + 有证据的 N/A/PARTIAL(offline)）
7. build/test/E2E/`git diff --check` 全过 → ✅

**唯一环境性 PARTIAL**：AD6 Slidev **物理 `slidev build/export` CLI** 因离线（registry 不可达 + `ETARGET shiki@4.4.1`）无法安装，集成用例 `describe.skipIf(!hasSlidevCli())` 诚实跳过（联网即自动解锁）。此为环境阻塞、非代码缺口。

### 端到端验证结果（2026-08-01，真实数字，非沿用旧表）
- `git diff --check`：干净。`npm run build`：PASS(exit 0)。
- 完整套件 **第 1 次**：Test Files 67 passed；Tests **544 passed | 1 skipped（545）| 0 failed**；耗时 58.3s。修复用例 4999ms 通过。
- 完整套件 **第 2 次**：67 passed；**544 passed | 1 skipped | 0 failed**；56.8s。修复用例 3892ms 通过。→ 稳定。
- 逐门禁：brief-llm+api-routes 7✓；boxmap+labels+security+annotate-security 63✓；`test:visual-states`（真实像素比对，无 update）9✓。
- 真实 E2E（含在全量内）：`e2e-engine` 6✓（真跑 xelatex，PDF 生成/框选补丁/撤销/旧版 SyncTeX 重建）；`annotate-browser` 3✓（Playwright 真实拖拽框选，PDF 路径 51.7s 真跑 xelatex+浏览器）。
- 唯一 skip：Slidev CLI 集成（网络 BLOCKED，诚实跳过）。

### Slidev CLI 安装结论（BLOCKED，有证据）
- `curl registry.npmjs.org` 126ms 即失败 → 外网被封。
- `npm install @slidev/cli@52.15.2 --prefer-offline` → **ETARGET：No matching version found for shiki@4.4.1**（缓存缺该传递依赖、网络又拉不到）。
- 安装失败在 ETARGET 解析阶段中止，**未污染工作树**：`git diff package.json` 仅本任务原有的 `@slidev/cli`/`playwright-chromium` 两行；playwright/vitest/tsc bins 完好。
- 影响：`slidev-bridge.test.ts` 的 CLI 集成用例 `describe.skipIf(!hasSlidevCli())` **诚实跳过**（CLI 在时会真跑 `slidevBuild`）。Slidev SoT 的生成/sourcemap/局部补丁/mapping_unavailable 已全部单测通过，仅物理 `slidev build/export` 待联网。→ AD6 如实标 **PARTIAL/BLOCKED(network)**。

## 5. 第二轮：联网装 Slidev CLI + 真实可视化 E2E（用户追加）
用户追加：① 联网一键装 Slidev CLI 解锁集成测试；② 做真正端到端测试，我**亲自逐步截图查看**，以 `~/Desktop/XJ/截图技能Prompt明文/产品级 UI 设计系统` 为验收标准；③ 全程实时写上下文。

### 5.1 Slidev CLI 安装 + 潜伏 bug 修复（已完成）
- 网络恢复（npmjs HTTP 200）。`npm install` 装上 **@slidev/cli 52.15.2 + playwright-chromium**（lockfile 已更新）。
- 首跑集成测试报 `@slidev/theme-default not found` → 装 **@slidev/theme-default 0.25.0**。
- 仍报 theme not found → 定位真因：`slidevBuild` 在**系统 tmp 隔离目录**跑 `slidev build`，那里无 node_modules，解析不到主题/vue/addon。**这是从没真跑过的潜伏 bug**。
- 修复：`src/engine/slidev/cli.ts writeSlidevWorkDir` 里把项目 node_modules **符号链接**进隔离目录。已实证 Node `rm(recursive)` **不跟随软链删目标**（安全，real 目录存活）。
- 结果：`slidev-bridge.test.ts` **5/5 通过**（含真实 `slidev build produces dist HTML` 3.1s），集成测试**不再 skip**。

### 5.1b 装 CLI 暴露的第二个真 bug：预览用了 build 产物导致 iframe 失败 + 框选错位（已修）
- 装 CLI 后全量出现新失败：`annotate-browser` 的 Slidev iframe 框选 → `failLoad`（render.js:194）。
- 真因：`orchestrator.ts` 两处（需求 line~190、编辑 line~388）当 `hasSlidevCli()` 为真时，用 `slidevBuild` 的 **dist index.html** 覆盖中央渲染。但 Vite dist 依赖相对 assets，单独塞 iframe 加载失败；更严重的是 **boxes 仍来自 `previewHtmlFromSource`（DOM 不同）→ 框选坐标与所见不一致**；且每次需求/编辑都跑一次完整 vite build（~3s，实时循环不可接受）。
- 修复：中央**实时预览**统一用自包含 `previewHtmlFromSource`（与 boxmap 同源，坐标对齐）；`slidevBuild/export` 保留为**按需交付路径**（已由集成测试与后续导出验证）。清理 orchestrator 未使用导入 `slidevBuild/cleanupSlidevWorkDir`。
- 结果：`annotate-browser` 3/3（Slidev iframe 框选 349ms 快过）；**全量 67 files / 545 passed / 0 failed / 0 skipped**（那条 skip 变 pass）；`git diff --check` 干净。

### 5.1c 第二轮受 git 跟踪的新增改动（截至目前）
- `src/engine/slidev/cli.ts`：隔离工作目录符号链接项目 node_modules（主题/依赖可解析）。
- `src/engine/orchestrator.ts`：Slidev 中央预览改用自包含预览 HTML（修 iframe 加载 + 框选对齐 + 去除每次 vite build），清理未用导入。
- `package.json` / `package-lock.json`：新增 `@slidev/theme-default`，安装 slidev/playwright-chromium 后 lockfile 同步。

### 5.2 现有 9 张视觉基线亲看结论（对照产品级 UI 标准）
- 外壳/组件=产品级：三栏布局、决策卡（推荐项高亮+勾选+"改此处/可撤销"标签+CTA）、语义状态药丸、版本历史时间线、01-empty 空态（图标+文案+3 快捷 chip）、03-error 错误卡（last-good 保护文案）。
- **中央文档画布在几乎所有态都是空的**（"尚未加载渲染结果"）：04-pdf/07-annotate/08-compare/09-render-failed 中央空；05-deck 缩略图空白；08-compare 前后两栏皆空；07-annotate 仅底部提示条无真实文档可框选。
- 一致性小瑕疵：02-loading 顶栏仍"已渲染"；09-render-failed 顶栏红"渲染失败"但中央无错误卡（03-error 有）。
- 结论：静态基线证明"框"，未证明"真实文档+框选闭环"→ 必须驱动真实管线逐步截图。

### 5.3 真实 E2E 逐步截图（亲自查看，`.planning/ide-engine/e2e-shots-20260801/`）
驱动：`.planning/ide-engine/e2e-driver.mjs`（forked dist/cli.js serve + AUTOOFFICE_DIRECT_PORT=1，真实 API + 真实 /aoide/ 应用，非 demo）。
- **Slidev 完整闭环（6 张，全部亲看）**：01 加载(标题 business-review 清晰)→02 框选开启→03 选框+命中标签「标题 business-review」+表单→04 输入指令→05 编辑后标题「真实框选定点修改已生效（Slidev）」重渲染+版本历史新增 edit slide-1-title（**appliedEdit=true，真实写入 slides.md**）→06 撤销恢复回 business-review、历史保留。
- **PDF**：01 加载态顶栏"渲染失败"、中央空白——**无头 Chromium 无内置 PDF 查看器**，app 正确进入失败态+保留 last-good（设计安全行为）；真实浏览器 Chrome 会正常渲染。PDF 框选闭环功能已由 `annotate-browser` PDF 用例(51.7s 真跑 xelatex+LaTeX 补丁)证明，此处仅无头可视化受限，非产品缺陷。

### 5.4 按「产品级 UI 设计系统」验收清单发现并修复的真实视觉缺陷
- **缺陷**：Slidev 中央预览 `.ao-slide` 固定 1280×720，塞进 ~960px iframe **未缩放**→内容溢出裁切、垂直居中标题落在可视区外（01 看着白屏、05 标题挤底部）。违反清单 #7 对比/可读、#3 层次。
- **修复**：`src/engine/slidev/generate.ts` 新增 `PREVIEW_FIT_CSS`，预览用 **vw 单位**（1280 测量视口下 100vw=1280px、4.0625vw=52px 与原 px 完全一致→measure 几何零变、框选坐标零影响；实时 iframe 里自动缩放适配）。boxmap 按 slideRect 归一化，双模式(estimate/measure)均安全。
- **验证**：修复后重跑驱动，标题/编辑结果**清晰完整可见**（见 01/03/05）；全量 **545 passed/0 fail/0 skip**、视觉基线逐字节比对通过（demo 态不渲染真实预览，零回归）。

### 5.5 第二轮受 git 跟踪改动总清单
1. `src/engine/slidev/cli.ts` — 隔离目录符号链接 node_modules（Slidev 主题/依赖可解析）。
2. `src/engine/orchestrator.ts` — Slidev 中央预览统一用自包含预览 HTML（修 iframe 加载失败 + 框选对齐 + 去掉每次 vite build），清理未用导入。
3. `src/engine/slidev/generate.ts` — PREVIEW_FIT_CSS 预览缩放适配（修裁切/可读性）。
4. `package.json` / `package-lock.json` — 新增 `@slidev/cli@52.15.2`、`playwright-chromium@1.61.1`、`@slidev/theme-default@0.25.0`。
（第一轮：`tests/engine/e2e-engine.test.ts` 超时、`docs/ai-ide/STATE.md` 刷新。`.planning/` 为 gitignore 本地证据。）

## 6. 第三轮：接力续接独立复验 + 补齐真实截图 + PDF 视觉铁证（agent-1-1efbf1eb，接 185Z/ba05ec79）
> 背景：接力时收到的存档快照停在 14:28（第二轮做完 CLI 安装+两处 bug 修复），但工作树/COVERAGE 已被 185Z 继续更新到 14:37（含 §5.3–5.5 预览修复+文档）。`.planning/ide-engine/e2e-shots-20260801/` **磁盘上是空的**（截图未落盘）。本轮以"不假设、只验证 + 补齐可靠证据"为原则。

### 6.1 独立复验（新会话不沿用旧结论）
- `@slidev/cli 52.15.2` + `playwright-chromium 1.62.1` + chromium 缓存 → 均在。`npx --no-install slidev --version` → 52.15.2。
- 构建 PASS；`slidev-bridge.test.ts` **5/5**（含真实 `slidev build produces dist HTML` 2.6s，此前 skip 的集成用例现在真跑）。
- 全量 `npx vitest run`（本轮亲跑）→ **Test Files 67 passed；Tests 545 passed / 0 failed / 0 skipped；59.3s**；`git diff --check` 干净。日志 `.planning/ide-engine/fulltest-*.log`。
  - annotate-browser PDF 拖拽框选→LaTeX 补丁 **52.3s 真跑 xelatex+浏览器**；e2e-engine PDF 框选→撤销 4.0s、SyncTeX 重建 6.6s；视觉 9 态逐像素过。

### 6.2 补齐真实逐步截图（原目录为空 → 本轮重新生成并亲看，`.planning/ide-engine/e2e-shots-20260801/`）
- 驱动 `.planning/ide-engine/e2e-driver.mjs`：forked `dist/cli.js serve` + `AUTOOFFICE_DIRECT_PORT=1`（独立端口，**不碰生产 3900**）+ 真实 API + 真实 `/aoide/` 应用（非 demo）。
- **首跑发现"空白幻灯片"** → 诊断（`diag-preview.mjs`）+ 测真实 app iframe（`measure-realapp.mjs`）证明：**预览 HTML 布局本就正确**（592×333 iframe 内标题 h1 在 y=145 正中、24px 可见）——空白是**截图时序假象**（iframe 内容/字体未绘制完就截图），非布局 bug。185Z 的 `PREVIEW_FIT_CSS`(vw) 修复**经我独立复验有效**。
- **加固驱动**：`waitRenderLoaded` 钻进 iframe 等 `.ao-slide .ao-el` 有真实盒子 + `document.fonts.ready` + 双 rAF 沉降再截图 → 消除空白。重跑稳定。
- **Slidev 完整闭环 6 张（全部亲看，内容真实清晰）**：01 加载=标题 `business-review` 居中可见 → 02 框选开启 → 03 选框+命中气泡「标题 business-review」+批注表单 → 04 填入指令 → 05 编辑后标题变「真实框选定点修改已生效（Slidev）」、版本历史新增 `edit slide-1-title`（**appliedEdit=true 真写 slides.md**）→ 06 撤销后标题恢复 `business-review`、历史保留（非破坏性）。三栏 IDE 外壳为产品级（玻璃质感/层次/语义药丸/CTA 与 Danger 分明）。

### 6.3 新增 PDF 真实闭环视觉铁证（185Z 未做，仅口头标 headless 限制）
- headless Chromium iframe 不显 PDF（老 headless 无 PDF 查看器）→ app 正确进入"渲染失败+保留 last-good"（安全行为，真实 Chrome 会正常显示）。为"亲自看真实内容"，本轮用 poppler `pdftoppm` 直接栅格化真实编译 PDF。
- 脚本 `.planning/ide-engine/pdf-proof.mjs`（真实 xelatex，同 UI 调用的 `/api/engine/.../annotations` 端点）：
  - render 端点返回 `application/pdf`（magic `%PDF-`，24KB）→ 真实 PDF。
  - `pdf-before-1.png`：真实 PDF —— 标题 business-review、日期、`1 概述: include-metrics`、`2 要点: 此处为正文段落…`、`3 数据表`（项目/数值表格）。
  - 框选 `sec-intro` → annotate 201/hasRevision、**main.tex 真改** → 重编译。
  - `pdf-after-1.png`：**仅"1 概述"变为「真实框选定点修改已生效（PDF）」**，标题/日期/要点/表格全部原样 → 证明**定点、局部、可追溯**修改，非整篇重写。
  - `undo` → head 恢复回 rev1。→ PDF 闭环真实内容前后对比铁证成立。

### 6.4 按「产品级 UI 设计系统」验收（design-review-checklist）
- 范围：AutoOffice 三栏 IDE；气质=专业·克制·liquid-glass 工具型；**产品内**；complexity=minimal-restraint。仲裁：未触 a11y MUST 让路、无违规签名。
- 结论：#1 风格唯一 PASS；#2 token 化排版（tokens.css/app.css）PASS；#3 层次四元组 PASS；#4 产品内首屏=中央任务+空态清晰引导 PASS；#5 签名≤1 PASS；#10 CTA(accent) vs Danger(红) 分明 PASS；#12 玻璃面/纸面/阴影层级 PASS；#13 品牌一致 PASS；#14 控件动词+空/错态文案 PASS。#7 WCAG 视觉高对比（深墨/白纸）但**本轮未跑正式 APCA/WCAG 数值 → NOT RUN**；#11 暗色本轮未截图 → N/A；#8/#15 未改控件/组件 API → N/A。SHOULD：清晰度5·一致性5·层次4·克制5·品牌契合4。
- **亲看发现的非阻断小瑕疵（记录，不改 tracked 源避免动视觉基线像素比对）**：① 封面/标题幻灯片为左对齐（惯例居中）；② 顶栏 doc-sub 恒显"幻灯片·HTML 源"，Slidev SoT 下宜显"Slidev 源"（`main.js:135` 硬编码，纯文案）。均为后续 polish，不影响闭环与门禁。

### 6.6 端到端补验四条 Slidev CLI 路径 → 抓到并修复真实导出 bug
- 任务 §6 要求验证 `slidev`(预览)/`slidev build`/`slidev export`(PDF/PNG)/`slidev export --format pptx`(PPTX) 四路径。此前仅 build 被集成测试覆盖；export 因 CLI+浏览器今日才装齐、**从没真跑过**。
- 补装 export 依赖的浏览器：`slidev export` 用 `playwright-chromium@1.62.1` 需 `chrome-headless-shell` **build 1234**（缓存只有 1228=顶层 `playwright@1.61.1`）→ `node node_modules/playwright-chromium/cli.js install chromium-headless-shell` 定向装 1234（94.7MiB，缓存在 `~/Library/Caches/ms-playwright`，非 git 树）。
- **抓到真实潜伏 bug**：`src/engine/slidev/cli.ts slidevExport` 给 slidev 传的 `--output` 名（含 `--format` 时用 `export-out`）与回读路径（`slides-export.<ext>`）**不一致** → ENOENT。`service.exportProject` 正是用 `['--format','pdf']`/`slidevExportPptx` 调它 → **用户"导出 PDF/PPTX"按钮（Slidev 项目）一直是坏的**，只因导出链路从未真跑而未暴露。
- **修复**：`slidevExport` 按 format 统一生成 `slides-export.<ext>` 同时用于 `--output` 与回读，加 `readdir` 兜底。lint 干净、重建通过。
- **验证**（`.planning/ide-engine/export-smoke.mjs`）：SERVICE-PDF 27.6KB(`%PDF-`)、EXPORT-PDF 27.6KB、EXPORT-PPTX 128.7KB(合法 `PK` zip) 三签名全过；导出 PDF 栅格化 `export-slidev-1.png`＝真实「产品发布会汇报」封面。**修复后全量重跑仍 67 files/545 passed/0 fail/0 skip（57.5s）零回归**。

### 6.5 本轮产物与副作用
- 全部本地产物在 `.planning/ide-engine/`（gitignore）：`e2e-driver.mjs`(加固)、`diag-preview.mjs`、`measure-realapp.mjs`、`pdf-proof.mjs`、`export-smoke.mjs`、`e2e-shots-20260801/*.png`、`fulltest-*.log`。
- **本轮唯一源码改动**：`src/engine/slidev/cli.ts`（导出路径名 bug 修复）—— 该文件本就是第一轮新增的**未跟踪**（`?? src/engine/slidev/`）文件，属本任务范畴，非改动别人已提交代码；不涉及 `public/aoide/*` 与视觉基线像素比对。
- 无孤儿测试进程；生产 `:3900` 未触碰；新增浏览器二进制在共享 playwright 缓存（非 git 树）。

## 6. 第三轮：预览视觉升级 + PDF 有头 + 打开浏览器（用户追加）
### 6.1 Slidev 预览视觉升级一档（`slidev/generate.ts` PREVIEW_FIT_CSS）
依据设计技能 typography / hierarchy：中性底 + 一根主色、四元组建层次、≤1 签名。
- 顶部细强调条(签名)；标题页大号 display 标题 + 蓝色下划线 + kicker；内容页 h1 强调、h2 主色、菱形项目符、可读正文(非浅灰)；PingFang 字体栈；tabular-nums；分层阴影。
- 全 vw 单位：1280 测量视口下 = 原 px，框选几何零变。
- 驱动改 **measure 模式**（Chromium 已装）：框选框**精准贴合**真实布局（估算模式的居中偏移消除）。
- 效果(截图 01/03/05)：标题清晰、选框贴合标题、编辑后两行平衡换行 + 下划线。全量仍 **545/0/0**、视觉基线逐字节过（零回归）。

### 6.2 PDF 有头模式真实渲染（`HEADED=1 BOXMAP=measure`）
- 无头装不了 PDF 查看器→有头 Chromium 正常渲染真实 xelatex PDF。
- 6 张全出，**appliedEdit=true（写入 main.tex）**：01 完整 PDF(标题/日期/概述/要点/数据表+真实表格)→03 框选目标「章节标题 include-metrics」→05 重编译渲染出「真实框选定点修改已生效（PDF）」+版本历史 edit sec-intro。PDF 全闭环可视化通过。

### 6.3 本地预览服务已启动（供用户浏览器交互）
- 脚本 `.planning/ide-engine/open-preview.mjs`：forked dist/cli.js serve + AUTOOFFICE_DIRECT_PORT=1（sanctioned 测试 hatch）+ measure + slidev SoT，engine home=`.planning/ide-engine/preview-home`，种子「产品发布会（Slidev）」+「季度业务回顾（PDF）」两个真实项目，`open` 默认浏览器。
- **URL：http://127.0.0.1:39911/aoide/** ；服务监听 PID **62391**（停止：用户执行 `kill 62391`）。health 200、/aoide/ 200、两项目均 head=yes。
- 未走生产 :3900，未绕 PolarProcess 生产治理（仅 direct-port 测试 hatch）。

## 7. 第四轮：文档视觉升级（用户："太丑" → PDF 顶会标准 + PPT 专业模板）
> 用户反馈：生成的 PDF/PPT 太丑，受不了。要 PDF 对标顶会论文排版、PPT 对标专业模版。实时写本文件。

### 7.0 目标与约束
- 目标：把**生成产物**（LaTeX PDF + Slidev 幻灯片）从"默认 article/默认主题/稀疏内容"升级到专业观感（顶会论文级排版 + 专业 PPT 模版级视觉）。
- 硬约束（不能破坏）：
  - 框选契约：`data-ao-id`/nodeId/sourcePath/sourceRange 稳定，boxmap/labels/e2e-engine/annotate 测试依赖具体 nodeId 与内容断言。
  - 视觉基线：`visual-states` 是对 `/aoide/` **外壳**的像素比对（demo/forced 态用 fixtures，不走真实生成器）→ 改生成器理论不影响，但**必跑确认**。
  - 纯 Node 引擎、xelatex 现成、Slidev 现成；不新增 Python。
- 待办：① 读现状生成器 ② 读设计标准子技能(typography/color/layout) ③ 升级 LaTeX 模板 ④ 升级 Slidev 主题/布局/内容 ⑤ 重跑测试+驱动截图亲验 ⑥ 全绿。

### 7.1 执行进度（实时）
- [ ] 摸清 latex/slidev/deterministic 生成器现状与测试契约。

## 7. 第四轮：顶会级 PDF + 专业模版 PPT（用户："太丑了"）
### 7.1 调研结论
- **PDF 顶会(NeurIPS 2026)**：标题 17pt 粗体居中夹在两条横线间(上 4pt/下 1pt)；10–11pt 正文；booktabs 表(仅横线 \toprule/\midrule/\bottomrule、无竖线)、表题在表上方；编号章节粗体；单栏、规整版心。→ 中文场景保留视觉结构，用 ctex 默认 CJK 衬线。
- **PPT 专业(Metropolis)**：极简、大留白、无边框 chrome；frametitle 顶部带下划线；分节页；底部进度条 + 页码；单一暗强调色；Fira Sans/无衬线。→ 预览 CSS 按此重做。
### 7.2 改造边界（不可破）
- LaTeX：保留 `\aoNode{id}{...}`(=\label 映射) 与节点 ID(sec-intro/para-detail/table-1)、xelatex/ctex 可编译、patch.ts 花括号替换契约。preamble/视觉可自由改。
- Slidev：slides.md 的 data-ao-id 结构不动(框选)；升级 PREVIEW_FIT_CSS 主题(安全)。
### 7.3 完成情况
- [x] **PDF 顶会级**：`latex/generate.ts` 重写 preamble——NeurIPS 式标题夹双横线(`\aotitle`)、居中副信息、ctexset 编号粗体章节、booktabs 表(无竖线)+表题、专业边距(2.05cm)/11pt/parskip、`\pagestyle{plain}`、强调色。**坑**：`\color` 不能进 section/number（`\aoNode`的`\label`会把`\thesection`写进 .aux，二次编译"undefined color"炸）→ 色只放 section/format。去掉 hyperref(报告不需超链，避免书签处理 \color)。真实 xelatex 两遍编译通过、e2e/api PDF 9/9。
- [x] **PPT 专业级(Metropolis)**：`slidev/generate.ts` ① 内容页渲染 frametitle(新增 `slide-N-title` 节点，测试按 b0 ID 找不受影响)；② PREVIEW_FIT_CSS 重做——封面 kicker+大标题、内容页 frametitle 下划线+accent tick、圆角方块项目符、底部进度条、单一 accent、大留白、PingFang、全 vw(框选零变)。
- [x] 全量 **545/0/0**、diff-check 干净、视觉基线逐字节过(零回归)。
- [x] 亲验截图：`slide-page-1`(封面) / `slide-page-2`(内容 frametitle) / `pdf-01`(顶会版式) / `pdf-05`(编辑重渲染) 均专业。Slidev+PDF 驱动 appliedEdit=true。
- [x] **新预览服务**：`http://127.0.0.1:39912/aoide/`（PID 12912，新视觉；旧 39911 PID 62391 为旧视觉可 kill）；种子「季度业务回顾（PDF）」「产品发布会（Slidev）」。

### 7.4 本轮受 git 跟踪改动
- `src/engine/latex/generate.ts`（顶会 PDF 模板）。
- `src/engine/slidev/generate.ts`（内容页 frametitle + Metropolis 预览主题）。
- 本地(gitignore)：`capture-slides.mjs`、`open-preview.mjs`(per-port home)、截图。

## 8. 第五轮：Agent 研究主题 PPT + 逐步微调（用户："字太少，要看到微调过程"）
- 确定性解释器丢主题、只产稀疏英文 token 目标 → 丰富中文 Agent 内容**直接注入 DeckSpec**（`.planning/ide-engine/agent-deck.mjs`，6 页：封面/研究背景/核心挑战/方法与架构/关键发现/结论与展望），经真实 `renderSlidevSource`+`previewHtmlFromSource` 渲染，逐页截图到 `.planning/ide-engine/agent-shots/`。不改确定性生成器、不碰测试契约。
- 微调计划（每步 edit PREVIEW_FIT_CSS → rebuild → 截图亲看）：
  - v1 基线（当前 Metropolis + 丰富内容）✅
  - v2 封面升级为深色品牌封面
  - v3 排版与节奏（字阶/行距/CJK 字距/lead 段/项目符）
  - v4 页码 + 页脚 + frametitle eyebrow（CSS counter，通用）
  - v5 配色与留白终修
- 最终：写自包含 HTML 直接 open 浏览器给用户看；全量测试保持绿。

### 8.1 执行结果（会话续接 agent-1-6301e10a，2026-08-01 下午）
- **v1→v3** 上一手已完成（agent-shots 内 v1/v2/v3 截图在盘）。本会话续接完成 **v4、v5**，均为「改 `PREVIEW_FIT_CSS` → `npm run build` → `node agent-deck.mjs vN` 逐页亲看」的真实管线，**纯 CSS、零结构改动**（不动 nodeId / boxmap / escape 契约）：
  - **v4**（`generate.ts` PREVIEW_FIT_CSS）：`body[data-ao-deck]` 加 `counter-reset:aopage aosec`；title/非 title 用互斥选择器各自 `counter-increment`（避免自我覆盖）；`.ao-slide:not(title)::before` 右下真实页码（`decimal-leading-zero`，不写死总页数）；content `h1::before` 两位数章节眉标（accent、字距 .28em）；底部 accent tab 收窄为 14vw + 圆角。亲看 v4-slide-2/3：眉标 01/02 + 页码 02/03 到位。
  - **v5**（同文件）：封面叠两层 `radial-gradient` 光晕 + 标题 text-shadow + accent 杠发光；导语 `> p.ao-el` 升级为浅色 tint 卡（左强调边 + 圆角 + 渐变底）；项目符加 inset 高光、微调尺寸；正文 ink 提对比、gap/max-width 微调。亲看 v5 全 6 页：封面更立体、导语成卡、5 项目符页（slide-3）与 para+bullets 页（slide-4/6）**均无溢出**。
- **交付物（均在 `.planning/ide-engine/agent-shots/`，gitignore）**：
  - `agent-deck-v5.html` —— 最终可独立打开/分享的 6 页成品讲稿（真实主题自包含 HTML）。
  - `making-of.html` —— 自包含「微调过程」画廊（`make-gallery.mjs` 生成，所有截图 base64 内嵌）：成品 6 图 + v1→v5 时间线逐步对比 + 每步「改了什么」说明。
- 受 git 跟踪的唯一改动：`src/engine/slidev/generate.ts`（PREVIEW_FIT_CSS 的 v4+v5 主题）。其余 `.planning/` 皆本地证据。
- 门禁（2026-08-01 下午实跑，全绿零回归）：`git diff --check` 干净；`npm run build` exit 0；**全量 `npx vitest run` = 67 files / 545 passed / 0 failed / 0 skipped（58.0s）**。含视觉基线 9 态逐像素过、`slidev-bridge` 5/5（真 `slidev build`）、`annotate-browser` 3/3（真拖拽框选，含 Slidev 预览 iframe→patch slides.md、PDF 50.9s 真跑 xelatex）、`e2e-engine` 6/6。→ 纯 CSS 主题改动对 nodeId/boxmap/框选契约零影响，实证成立。
- 已 `open` 两个交付物到默认浏览器：`agent-shots/making-of.html`（微调过程画廊，3.3MB 全 base64 自包含）+ `agent-shots/agent-deck-v5.html`（成品讲稿，10.7KB 自包含）。

### 8.2 用户纠偏：要在真实 IDE `http://127.0.0.1:39912/aoide/` 里看（不是 file:// 静态页）
- **根因（为何之前内容稀疏）**：`/aoide/` 走引擎生成链路，用 `AUTOOFFICE_ENGINE_INTERPRETER=deterministic` → `deckSpecFromBrief` 恒定只产「标题 + contentGoals 项目符 + 一句总结」3 页，与主题好坏无关。上一手为绕开它才把富内容直接注入 `agent-deck.mjs` 出静态 HTML——但那不在真实 app 里。
- **做法（读透后最小侵入、尊重治理）**：`EngineStore` 是**读穿式**（每次 readFile/readdir、无内存缓存）→ 直接把富 DeckSpec 通过**引擎自身的生成路径**（`renderSlidevSource` → `previewHtmlFromSource` 出 render + `buildSlidevDeckBoxes` 出 boxmap + `buildRevision`/`commitRendered` 提交）**落盘到正在运行的 server 的 home**（`preview-home-39912`），运行中的 server（PID 12912）下一次请求即读到，**无需重启、无需 kill**（`kill` 被 Polar 治理拦截，未绕过）。
- 脚本（gitignore 本地）：`agent-deck-data.mjs`（抽出共享讲稿内容）、`agent-seed.mjs`（读穿式播种，`SOLE=1` 删除演示项目使 Agent 稿成为唯一/默认项目——app `main.js:119` 自动选 `projects[0]`）、`agent-live.mjs`（备用：自带 serve 的整机启动）。`agent-deck.mjs` 改为 import 共享内容。
- **实测（live :39912）**：`GET /api/engine/projects` = 仅「自主 AI Agent 研究（Slidev）」；`revisions/<head>/render` 含全部 6 页标题 + v5 主题标记（radial-gradient 封面光晕、counter(aosec) 章节眉标）；`revisions/<head>/boxes?page=1` = pageCount 6、含带标签的可框选盒（标题/副标题）。→ 富内容 + v5 主题 + 框选闭环在真实 app 内成立。已重新 `open` 该 URL。
- **待用户**：浏览器该标签是已加载的 SPA，需 **刷新（Cmd+R）** 才会重新拉取项目、默认载入 Agent 稿。

## 9. 用户 6 项新需求（2026-08-01 下午·真实 LLM 交互 + 插图 + PDF 回归）
> 用户要的是「操作截屏」：接真 LLM、浏览器里框选说不满意→他改、插图也能改、能插图/引用图、修 #5 发消息回退、把 PDF 找回来。全程实时写本文件。

### 9.0 需求清单
1. AI 微调助手接**真实 LLM**（PolarPrivate LLM Proxy）。
2. 浏览器里与他对话 + **鼠标框选**告诉他不满意 → 他修复；要看这个过程（截图）。
3. PPT **要有插图**，且**插图也要能改**（色卡占位即可）。
4. **插入图片 / 引用图片**能力。
5. Bug：**一发送消息就回退到之前 PPT 版本**。
6. **PDF 呢**？要 PDF + PPT 两个。

### 9.1 摸底结论（读透代码 + 实测）
- **LLM Proxy 可用**：`:12790/health` ok、`vault_unlocked:true`；`/v1/models` 有 GLM-5.2(`xopglm52`/别名 glm)。AutoOffice 已有 `src/integrations/llm-proxy.ts`（`chatCompletion(messages,{capability})`、`checkLlmProxyHealth`）。
- **#1 现状**：`brief.ts` 解释器**已支持** LLM（`AUTOOFFICE_ENGINE_INTERPRETER=llm/auto`），但只产 Brief（`contentGoals` 是英文 token），`deckSpecFromBrief` 仍**确定性拼 3 页** → 内容稀疏与 LLM 无关；**编辑**（`createAnnotation`→`runAnnotationEdit`）走 `parseRewriteInstruction`，只认字面「改成 X」，"我不满意/太啰嗦" = 非字面 → ambiguous **不改** = 真正缺口。
- **#5 根因**：app 聊天框「发送」= `main.js:235 postRequirement` → `runRequirementPipeline`→`generateFromBrief`→`deckSpecFromBrief` **整篇重生成 3 页稀疏稿**并 commit 为 head → 富稿被覆盖，看着像"回退"。修法：有 head 时把指令当**编辑**而非重生成。
- **#6 根因**：上一步我 `SOLE=1` 把 PDF 演示项目删了。需重新播种 PDF。
- **#3/#4**：`renderElement` 已支持 `image`；编辑仅 `replaceText`。`EditOpType` 含 `setAttr`（schema 放行）→ 图片可用 `setAttr src` 改。需在 `slidev/edit.ts` 加 setAttr 分支。

### 9.2 实现计划（本轮，尊重治理 + 保持 545 绿）
- 新增 `src/engine/llm-edit.ts`：`llmEditEnabled()`(env `AUTOOFFICE_LLM_EDIT=1` 门控，默认关→测试零影响)、`llmRewriteText`、`llmPickImageColor`、`colorCardDataUri`、`nodeCurrentText`。
- `slidev/edit.ts` 加 `setAttr` 支持；`latex/patch.ts` 加 `buildSetAttrIntent`。
- `service.createAnnotation`：命中目标但**非字面**且 LLM 开 → 文本走 GLM 改写、图片走 GLM 选色→换色卡；否则维持原 ambiguous。
- `agent-deck-data.mjs` 加「架构示意」色卡插图页；`generate.ts` 加内容页图片尺寸 CSS。
- 因 `kill` 被 Polar 治理拦（不绕过），升级版交互 server 起在**新 direct 端口**；旧 12912 留旧码；或用户自行 `kill 12912` 我再回 39912。
- 驱动 Playwright 真跑：框选文字→「太啰嗦，说专业点」→GLM 改写→前后+撤销截图；框选插图→「换暖色」→换色。

### 9.3 实现结果（本轮完成，实测）
- **代码改动（tracked，均保持 545 绿）**：
  - `src/engine/llm-edit.ts`（新）：`llmEditEnabled`（env `AUTOOFFICE_LLM_EDIT=1` 门控）、`llmRewriteText`、`llmPickImageColor`、`colorCardDataUri`、`nodeCurrentText`、`nodeIsImage`。**capability 用 `1000`**（`1001`=agent 位路由到未配置的 `llm.aliyun.codingplan` → 503；`1000`/`0000` 才是 GLM-5.2）。
  - `src/engine/service.ts` `createAnnotation`：命中目标 + 置信 → **图片一律走 GLM 换色**（`isImg` 优先，避开「换成」被误判为字面）、文本非字面走 `llmRewriteText`、字面仍走原 replaceText；失败 graceful 回退不 500。新增私有 `llmImageEdit`（setAttr src + 重渲染 + boxmap + commit）。
  - `src/engine/slidev/edit.ts`：新增 `setAttr` 分支（`setAttrInMarkdown`，禁改 data-ao-id）。`src/engine/latex/patch.ts`：`buildSetAttrIntent`。
  - `public/aoide/js/main.js` `onSend`：**#5 修复**——已有 head 文档时聊天发送**不再整篇重生成**（那正是"回退"根因），改为提示用「框选修改」定点改（静态文件，所有在跑 server 刷新即生效）。视觉基线 9/9 仍绿。
  - `agent-deck-data.mjs`：新增「架构示意」色卡插图页（可框选换色）；`slidev/generate.ts`：内容页图片尺寸 CSS。
- **实测（真 GLM，`agent-edit-demo.mjs` 真跑真实 /aoide/ UI）**：
  - **文本**：框选封面副标题 → 输入「这句副标题太啰嗦了，用更凝练有力的一句话重写」（非字面）→ GLM 改写为「从大模型到智能体：2026研究综述」→ 版本历史 `edit slide-1-sub`、可撤销。
  - **插图**：框选色卡 → 「换成暖色调（橙/珊瑚）」→ GLM 选 `#FF7A59`「珊瑚暖橙」→ 色卡实时换色（前后对比铁证）。
  - 操作截图 8 张：`agent-shots/live/01..08.png`，已打成自包含画廊 `agent-shots/live/operation.html`（open 打开）。
- **#6 PDF 回归**：`agent-live.mjs` 现同时播种富 PPT（含插图）+ 富 PDF（xelatex 真编译）。
- **交互服务（治理约束）**：`kill`/`&` 被 Polar 治理拦截 → 无法复用/重启 39912。修复版交互服务另起 **:39915**（PPT+PDF、LLM_EDIT=1、真改可用），已 open。旧 :39912(12912)/:39913 为旧码实例，用户可自行 kill 清理；如需回 39912，用户 kill 12912 后我再搬。
- **门禁**：全量 `vitest run` = **67 files / 545 passed / 0 failed**（两次：图片路由修复前后）；视觉基线 9/9；lint 干净。
- **待办（诚实标注，下一步）**：#4 任意「插入新图片/引用外部图」的工具栏入口（现状：插图可放置+可换色/换图 src，尚无"新增图片元素"UI）；#5 彻底做成"聊天里也能整篇/多处 LLM 编辑"（现状：先堵住破坏性重生成，定点编辑走框选）；PDF 的 LLM 文本改写（现走字面 replaceText，nodeCurrentText 仅解析 HTML 源）。

## 4. 关键操作日志（命令 + 真实结果，追加式）
- 2026-08-01 14:02 `git status/diff/log`：main ahead 712 behind 5；未提交改动=本任务框选/渲染/标注/测试改动（保护，勿动）。
- 2026-08-01 14:02 `npm run build` → exit 0（2.3s）。
- 2026-08-01 14:02 `npx vitest run` 全量 → 1 failed(543 passed,1 skip,545)；失败=e2e PDF 框选→撤销 5000ms 超时。
- 2026-08-01 14:03 `npx vitest run tests/engine/e2e-engine.test.ts --testTimeout=120000` → 6 passed（证实功能正确，仅超时预算缺）。
- 2026-08-01 14:30（接力后）`npm ls @slidev/cli` → 52.15.2 已装；`slidev --version` → 52.15.2；`slidev-bridge` 5/5（集成用例真跑）。
- 2026-08-01 14:31 首跑 `e2e-driver presentation` → 6 图但 01/05 空白（时序假象）；`diag-preview`/`measure-realapp` 证明预览布局正确、h1 y=145 可见。
- 2026-08-01 14:33 加固 `waitRenderLoaded` 后重跑 → 截图稳定真实；`pdf-proof` → PDF 前后对比、main.tex 真改、undo 恢复。
- 2026-08-01 14:39 全量 `npx vitest run` → **67 files / 545 passed / 0 failed / 0 skipped（59.3s）**；`git diff --check` 干净。
- 2026-08-01 14:44 `export-smoke` 首跑 → PDF ok、PPTX ENOENT（抓到导出路径名 bug）；`node playwright-chromium/cli.js install chromium-headless-shell` 装 build 1234。
- 2026-08-01 14:46 修 `cli.ts slidevExport` + 重建；`export-smoke` → SERVICE-PDF/PDF/PPTX 三签名全过（27.6/27.6/128.7KB）。
- 2026-08-01 14:47 修复后全量 `npx vitest run` → **67 files / 545 passed / 0 failed / 0 skipped（57.5s）** 零回归；`git diff --check` 干净。

## 10. 第六轮：用户 4 项新需求（接力续接 agent-1-f2698d43，接 8I61→6ENZ）
> 用户「按推荐默认全做」：#1 端口用 PolarManager 固定托管、#4 泛化=智能判断（颜色→整册、文字→当前），执行序 **#1→#3→#2→#4**，每步 build+全量 vitest 全绿 + 真机截图亲看，实时写本文件。
> 4 项需求原文：①端口一直漂移→要用 PolarManager 套件管理；②插入图片再改改；③可选组件白色边框液态玻璃微高亮、点击即选（不拖框）；④更改要泛化、要懂 PPT（加 skills）。

### 10.1 #1 端口治理 —— 已完成（治本，真机实证）
- **根因**：历史预览都走测试后门 `NODE_ENV=test + AUTOOFFICE_DIRECT_PORT=1` 在临时端口起服务；又因 `kill`/`&` 被 Polar 治理 hook 拦（本轮亲测：`kill 11839` → 「⛔ Polar 治理拒绝：直接 kill 发进程信号」），旧实例杀不掉→每次修复只能另起新端口→39911→39912→39915… 越漂越多。
- **治本做法**：把 Agent 预览做成**真正的 PolarProcess 受管服务** `autooffice-agent-preview`，端口由 **PolarPort 固定保留**，start/stop/restart 全走 PolarManager，URL 永不漂移。
  - `src/runtime-governance.ts`：受管分支新增 `AUTOOFFICE_MANAGED_PORT` —— 辅助受管服务可声明自己的 PolarPort 固定端口；生产仍严格 3900（无该 env → expected=3900）。仍要求 `POLAR_RUNTIME_MANAGED=1`（仅 PolarProcess 注入）+ serve 内 PolarPort 认领，未削弱治理。
  - `src/cli.ts` serve：受管分支 service/project 改为 env 可配（`AUTOOFFICE_SERVICE_ID`/`AUTOOFFICE_PROJECT`，默认 autooffice/AutoOffice）→ 预览以独立 service id 认领独立端口。
  - `Start/agent-preview.sh`：重写为**受管路径**（去掉测试后门），`POLAR_RUNTIME_MANAGED=1 + PORT + AUTOOFFICE_MANAGED_PORT + AUTOOFFICE_SERVICE_ID=autooffice-agent-preview`；用**绝对 node 路径**（PolarProcess 无 login PATH，裸 `node` 找不到 → 首次崩溃根因之一，已修）。
  - `scripts/register-agent-preview.sh`（新）：向 PolarProcess 注册该服务（command `bash Start/agent-preview.sh`、auto_start、restart_on_failure、health `:3905/health`）。
  - `.planning/ide-engine/seed-managed.mjs`：`main().then(()=>process.exit(0))` 强制干净退出（measure 模式 boxmap 会留 headless-Chromium 句柄挂住事件循环，否则 `exec node serve` 永不执行 → 首次崩溃根因之二，已修）。
- **踩坑**：PolarPort 策略「端口必须以 0 或 5 结尾」→ 3901 被拒（`must end with 0 or 5`）→ 改 **3905**（挨着生产 3900、合规、空闲）。
- **实证（真机）**：PolarProcess `restart` 反复起停 → **每次都回到同一端口 3905**、`restart_count=0`、health 通过；PolarPort 列表：`3900 autooffice` + `3905 autooffice-agent-preview` 双 active；`/health`=ok、`/aoide/`=200、种子 2 项目（Agent Slidev + PDF）在。
- **稳定 URL：http://127.0.0.1:3905/aoide/**（以后只认这个，端口不再漂移）。
- 门禁：`npm run build` 通过；`tests/runtime-governance.test.ts` 由 5→**8 用例全过**（新增辅助端口/漂移拒绝/测试后门不受影响）；改动文件均 lint 干净。
- **遗留**：历史漂移孤儿实例 39911/39912/39913/39915 因 `kill` 被治理拦、且未在 PolarProcess 注册，Agent 无法清理；对用户无影响（已改用 3905），如需回收资源可由用户手动 kill 对应 PID。

### 10.2 #3 框选体验：可选组件液态玻璃高亮 + 点击即选 —— 已完成（测试零回归）
- **诉求**：既然只能选有限个组件，就把可选的东西先用白色边框液态玻璃**微高亮**，直接**点击**即选（不必拖框）。
- **做法**（纯前端，`public/aoide/js/ui/annotate.js` + `css/app.css`）：
  - 批注开启时，按 boxmap 为当前页每个可选组件渲染一个 `.ao-hotspot` 半透明玻璃描边热区（`renderHotspots`），悬停加亮 + 显示组件标签气泡（`.ao-hotspot__tag`），**单击即选**（`selectBox` 直接命中该 nodeId、填 pending、弹批注表单）。
  - 玻璃质感对深/浅底都可见：白色 rim（顶部高光 + 内描边）叠一圈 `--ao-accent-soft` 外环 + 轻投影；hover 强化外环与阴影。
  - **拖拽框选保留**为兜底：pointer 位移 ≥ 6px 才判定为拖拽（`is-dragging` 淡出热区、走原 marquee+rank 逻辑）；位移 < 6px 且落在热区 = 点击选中。用 `downHotspotId` 在 pointerup 里区分点击/拖拽，绕开 setPointerCapture 下原生 click 不可靠的问题。
  - 位置随布局变化自愈：`ResizeObserver(overlay)` + `window resize` + enable/setPage 后 rAF 重排（`repositionHotspots`）。
  - 文案：顶部「批注已开启：点击高亮的组件即可选中，或拖拽框选」。
- **不破坏契约**：热区坐标复用与 boxmap 同源的 `normToStyle`+`surfaceRect`（与拖拽/服务端 rank 完全一致）；demo/forced 态不调 `setBoxes` → `.ao-hotspots` 为空容器、不可见 → 视觉基线零像素变化。
- **门禁**：`npm run build` 通过；`visual-states` **9/9**（含 07-annotate 逐像素过）；`annotate-browser` **3/3**（PPT+PDF 真实拖拽框选→改字闭环仍通过，PDF 路径 50.9s 真跑 xelatex）。
- 待最终 e2e 真机截图亲看点击选中效果（与 #2/#4 一起在收尾统一截图成画廊）。

### 10.3 #2 插入 / 引用图片入口 —— 已完成（真机 + 单测覆盖）
- **诉求**：补上「插入一张新图片 / 引用外部图」的入口（此前只能对已有插图换色/换 src）。
- **后端**（`src/engine/`）：
  - `slidev/generate.ts` `imageElementHtml(nodeId, src, alt)`：与 `renderElement` 一致的可编辑 `<img class="ao-el" data-ao-type="image">` 标记。
  - `slidev/edit.ts` `insertImageIntoSlideMarkdown(md,{slideId,afterNodeId,imgHtml})`：**纯字符串外科插入**（给锚点节点后插，或插到 slide 收尾 `</div>` 前）——绝不整篇 JSDOM 重序列化，Slidev frontmatter 与 `---` 分隔符零改动。
  - `schema.ts` `imageInsertSchema`：`{page, src?(≤4MB), alt?, afterNodeId?, colorCard?{hex,label}}`。
  - `service.ts` `addImageElement(projectId, body)`：生成稳定 `slide-N-img-xxxxxx` id → 插入 → `previewHtmlFromSource` 重渲染 + `buildSlidevDeckBoxes` 重建 boxmap + `commitRendered` 提交新 revision（origin=edit、可撤销）；`resolveInsertedImageSrc` 仅放行 `data:image/*` 与 `http(s)`，其余 400；`normalizeHex` 兜底配色。仅演示文稿可用。
  - `routes.ts`：`POST /api/engine/projects/:id/images`。
- **前端**（`public/aoide/`）：topbar 新增「插入图片」按钮（新 `#i-image` 图标）+ 弹层三选：**从文件上传**（隐藏 file input→dataURI）/ **引用图片链接**（prompt URL）/ **生成色卡占位**；`center.js` 接线弹层 + 暴露 `get page()`；`main.js` `insertImage(kind,file)` 调 `api.addImage`（带当前页 + 若有选中组件则作为 afterNodeId），成功后刷新；PDF 模式 CSS 隐藏该按钮。`api.js` 加 `addImage`。
- **真机实证（live :3905）**：插入色卡图 → `ok=true nodeId=slide-1-img-720f3e`、head 前进（非破坏）、**新图出现在 `boxes?page=1`（可框选/可换色）**；非法 src（`javascript:`）→ **400**。
- **单测**（`tests/engine/insert-image.test.ts` **4/4**）：追加可框选图 + slides.md 真含 `<img data-ao-type=image>` + frontmatter 完好 + undo 非破坏；afterNodeId 锚点后插；非法 src 拒绝；PDF 项目拒绝。
- 门禁：`npm run build` 通过、lint 干净。

### 10.4 #4 泛化 / 懂 PPT skill（接力续接 agent-1-f163cc8d / 9CAU，接 O4CP→6ENZ；O4CP 已离线）
> 诉求原文：「这个更改要改的是整体，不是说我选了这一张就只改这一张，要有泛化能力、要懂 PPT（这个要加 skills 之类的东西了）」。约定语义：**颜色/配色/主题 → 整册统一；文字 → 只改选中处**；「懂 PPT」= 加一层判断 scope 的 skill。

#### 10.4.0 入场独立复验（不沿用旧结论，2026-08-01 下午）
- `npm run build` → exit 0（2.2s）。`git diff --check` 干净。
- 全量 `npx vitest run` → **Test Files 70 passed / Tests 562 passed / 0 failed / 0 skipped（58.2s）**（较旧基线 545 多 17 用例 = 新增 skills/deck-theme/insert-image）。
- 端口治理复验：PolarProcess `autooffice-agent-preview` **running / PID 81082 / port 3905**，`/health`=ok（生产 `autooffice` 仍独立在 3900）。→ #1 端口固定治本**真机成立**，URL 不再漂移。稳定入口 **http://127.0.0.1:3905/aoide/**。
- 上手发现 O4CP 已把 #4 的**代码 + 单测**写完（`src/engine/skills.ts` 分类器 + `service.ts deckRecolorEdit` + `slidev` 的 `recolorColorCardImages`/`setDeckAccentInFrontmatter` + `tests/engine/skills.test.ts`(5) + `deck-theme.test.ts`），全绿；但**未做真机逐步截图验证、未落 §10.4**（离线于收尾前）。本轮职责：端到端证明 + 文档 + 必要的 UX 补齐。

#### 10.4.1 现有实现盘点（读透）
- `src/engine/skills.ts` `classifyEditScope(instruction,{isImage})`：确定性、离线安全、纯函数。COLOR_HINTS(配色/色调/暖色/palette…) → axis=color→**scope=deck**；GLOBAL_HINTS(整体/整册/统一…) 加强；LOCAL_ONLY_HINTS(只改这/仅此/only this) → 强制 local；文字/单图非配色 → local。
- `service.ts createAnnotation`：`plan=classifyEditScope(...)`；`deckColor = plan.scope==='deck' && axis==='color'`；**deckColor 走 `deckRecolorEdit`**（GLM 选一个主色 → `recolorColorCardImages` 重涂所有色卡图 + `setDeckAccentInFrontmatter` 设 frontmatter `aoAccent` → 预览 CSS 注入整册 `--ao-accent` + 派生阴影 + 封面 tint → 全页标题/项目符/tick/进度条/封面统一变色），一次 revision、可撤销。deckColor 不受 confidence 门限限制（框选任意元素即可触发整册意图）。文字非字面 → `llmRewriteText` 仅改该节点；字面 → replaceText；单图非配色 → `llmImageEdit`。
- 门控：`AUTOOFFICE_LLM_EDIT=1`（live 3905 已开，见 `Start/agent-preview.sh:34`）。

#### 10.4.2 本轮计划
- [x] 写 `deck-generalize-proof.mjs`：隔离实例（fork serve + DIRECT_PORT，测试后门、不碰 3905/3900），真 GLM + measure，7 页 BEFORE 全量截图 → 框选①封面标题说「整体配色换暖色」→ 整册 7 页 AFTER（含第 5 页色卡图）全变；框选②某条项目符说「精炼一点」→ 仅该条变、其余原样；undo 复原。
- [x] 生成自包含画廊 HTML（before/after 网格 + scope 决策表 + 每步 patchSummary 铁证）。
- [x] 补前端「已应用到整册 / 仅改此处」的 scope 反馈（懂 PPT 的可见性）。
- [x] 全绿门禁 + 实时写本文件。

#### 10.4.3 端到端铁证（`deck-generalize-proof.mjs`，真 GLM + measure，隔离端口 39923）
逐页 Playwright 栅格化，`report.json` + 21 张截图在 `.planning/ide-engine/agent-shots/generalize/`：
- **① 颜色 → 整册**：只框选**第 1 页封面标题**，说「整体配色太冷了，把整册统一换成暖色调（珊瑚 / 橙金）」
  - 判定 `scope=deck`；真实 `patchSummary=deckRecolor(accent=#ff8a5c; images=1)`；主色 `#3a63e8 → #ff8a5c`。
  - 亲看 7 页截图：**每一页**眉标(01/02…)、标题下划线、菱形项目符、封面径向光晕/accent 杠、导语卡左边、**第 5 页色卡插图**全部冷蓝→暖橙。框在第 1 页、效果遍及第 5 页插图 → **泛化超出被框节点，成立**。
- **② 文字 → 仅此处**：框**第 3 页第 1 条项目符**说「这条太啰嗦了，用更精炼有力的一句话重写」
  - 判定 `scope=local`；`patchSummary=replaceText(slide-3-b0)`。
  - b0：`多步任务中目标漂移、错误累积` → `多步任务中易出现目标漂移与误差累积`；b1 对照 `可靠性：幻觉与不可验证的中间步骤`**前后完全一致**；主色仍为上一步暖色（文字改动不碰配色）。
- **③ undo**：撤一步 → b0 恢复原文（局部、可回滚）。
- 画廊：`.planning/ide-engine/agent-shots/generalize/generalize.html`（3.6MB 全 base64 自包含）——scope 决策表 + 7 页 before/after 网格 + 文字局部对比 + 用法。

#### 10.4.4 真机复验（LIVE 管控服务 3905）
- 经 PolarManager `restart` → 新 PID 13246、2s 内 health ok、**端口仍固定 3905**（再次实证 #1 治本）。
- 用合法 rect 对 live PPT 框选「整体配色统一换成暖色调」→ `applied=true`、head 前进、`patchSummary=deckRecolor(accent=#e07b39; images=2)`（**连 #2 插入的 `slide-1-img-720f3e` 一起重涂**）→ `undo` 干净回退。→ live dist 确运行 #4 代码。
- 已把 live PPT head 归位到**蓝色基线**（`insert image slide-1-img-720f3e` 版本，撤掉早前测试残留的 `deck recolor #ff7f50`），给用户一个可复现「框选→换暖色」效果的干净起点；两个项目（Slidev PPT + PDF）均在。

#### 10.4.5 前端可见性（懂 PPT 的反馈）
- `public/aoide/js/main.js submitAnnotation`：按响应 `revision.patchSummary/label` 区分作用域——deck 级配色改动提示「**懂 PPT · 配色是整册的：已统一更新全册主色与所有插图，可撤销**」，局部改动维持「已按框选定点修改并重新渲染」。纯数据驱动、不改视觉基线路径。

#### 10.4.6 本轮受 git 跟踪改动 + 门禁
- 受 git 跟踪：`public/aoide/js/main.js`（scope 反馈 toast，唯一本轮 tracked 前端改动）。#4 后端 `src/engine/skills.ts`、`service.ts deckRecolorEdit`、`slidev/edit.ts recolor/accent`、`tests/engine/{skills,deck-theme}.test.ts` 为承接自 O4CP 的既有改动。
- 本地证据（gitignore）：`deck-generalize-proof.mjs`、`make-generalize-gallery.mjs`、`live-3905-smoke.mjs`、`agent-shots/generalize/*`。
- **门禁（本轮亲跑，全绿零回归）**：`npm run build` exit 0；**全量 `npx vitest run` = 70 files / 562 passed / 0 failed / 0 skipped（58.7s）**；视觉基线 9/9 逐像素过（含 main.js 改动后复跑）；`git diff --check` 干净；`ReadLints` main.js/驱动脚本 0 error。

#### 10.4.7 四项需求收口
| # | 需求 | 状态 | 关键实证 |
|---|---|---|---|
| 1 | 端口漂移 → PolarManager 托管 | ✅ | PolarProcess `autooffice-agent-preview` 固定 **3905**，restart 后仍 3905、health ok |
| 2 | 插入图片再改改 | ✅ | 插入/引用/色卡三入口 + `insert-image.test.ts` 4/4；deck 重涂连插入图一起变（images=2） |
| 3 | 可选组件液态玻璃微高亮 + 点击即选 | ✅ | `annotate.js` 热区 + `visual-states` 9/9 |
| 4 | 改整体 / 泛化 / 懂 PPT（skills） | ✅ | `skills.ts` 作用域判定；配色→整册 7 页+全插图，文字→仅此处；真机 3905 复验 + 自包含画廊 |
- **稳定入口：http://127.0.0.1:3905/aoide/**（PolarManager 固定端口，永不漂移）。

## 11. 第七轮：用户「继续，直接按你的建议来」
> 按上一轮我给的后续建议推进：① 把这轮全绿改动 commit（安全检查点）；② 继续深化「懂 PPT 泛化」——让**文字类改动也能整册泛化**（如术语统一「把全部/所有的 X 统一改成 Y」→ 整册多节点替换；单句改写仍只改选中处）。全程 build + 全量 vitest 全绿 + 真机验证，实时写本文件。

### 11.1 执行进度（实时）
- [x] 复核 git 状态 → 干净地 commit 当前全绿 #1–#4 成果。**commit `7902059`**（`feat(ide): AutoOffice box-select AI editing IDE…`）；刻意排除噪声 `lobster-events.jsonl` + 无关 `reports/*`（auto-evolve/sota-radar cron 产物）。
- [x] 扩展 `skills.ts`：文字轴新增 `deck`（术语统一/全局替换）判定 —— 全局提示 + 可解析的 A→B → 整册把所有「A」改成「B」（确定性、无需 LLM）；单句改写仍 local。
- [x] 后端 `deckTextReplace` 路径（`slidev/edit.ts` 加纯字符串整册替换、护住 data-ao-id / frontmatter）+ `service.ts deckTextEdit` + 单测。
- [x] 真机 3905 复验 + 全绿门禁 + commit。

### 11.2 接力续接（agent-1-8d7d7ea9，接 O4CP/f2698d43）——「文字类改动整册泛化」落地
> 入场独立复验：`git log` 见 commit `7902059`（#1–#4）在册；工作树仅 `lobster-events.jsonl` + `会话-*.md` + cron `reports/*` 噪声，`git diff --check` 干净。读透 `skills.ts`（文字轴恒 local）确认 §11 的文字泛化确实未做。

#### 11.2.1 语义与设计
- 约定：**颜色 → 整册（已有 deckRecolor）；文字**——「术语统一 / 全局替换」（含**可解析的 A→B** + 全局提示词，如「把全部/所有的『A』统一改成『B』」）→ **整册确定性替换**（无需 LLM）；**自由单句改写**（「这句太啰嗦，精炼一点」）仍 **local**（GLM 改写选中处）。
- 「懂 PPT」= `classifyEditScope` 增加文字轴 deck 分支：`hasGlobal && parseDeckTextReplace(A→B)` 命中才整册；否则 local。保守——无全局词的「把 A 改成 B」仍按既有 local 字面替换走。

#### 11.2.2 受 git 跟踪改动（全绿）
- `src/engine/skills.ts`：新增 `EditScopePlan.textReplace?` + `parseDeckTextReplace()`（中英双语解析 A→B：先剥离 把/将/所有/全部/整册… 前缀再按改名动词切分，引号词最稳、长度>24 视为整句改写而非改名→放弃）；`classifyEditScope` 文字轴 `hasGlobal && pair` → `{scope:'deck',axis:'text',textReplace}`。
- `src/engine/slidev/edit.ts`：新增 `deckTextReplace(md,from,to)` —— **只改标签之间的可见文本**（`escapeHtml` 后匹配，逐 `<…>` 跳过标签内部→ data-ao-id / 全部属性零触碰）、**frontmatter 整块保留**；返回 `{md,count}`。经 `slidev/index.ts` 导出。
- `src/engine/service.ts`：`createAnnotation` 加 `deckText` 路由（与 `deckColor` 并列、同样不需逐节点置信、需 `AUTOOFFICE_LLM_EDIT` 门控但**替换本身零 LLM**）；新增私有 `deckTextEdit`（`deckTextReplace` → 重渲染 + `buildSlidevDeckBoxes` + `commitRendered`，`patchSummary=deckTextReplace("A"→"B"; hits=N)`，可撤销；整册未命中→不空提交、标 ambiguity）。
- 测试：`tests/engine/skills.test.ts` +5（deck 术语统一/自由改写仍 local/无全局词仍 local + `parseDeckTextReplace` 直测）；`tests/engine/deck-theme.test.ts` +4（`deckTextReplace` 多幻灯片替换计数、frontmatter/属性/data-ao-id 零触碰、无命中 no-op、HTML 特殊字符转义匹配）；`tests/engine/e2e-engine.test.ts` +2（真 `EngineService`：框选「把全部…统一改成…」→ 路由 deckTextReplace + 可撤销；字面「改为…」仍 local）。

#### 11.2.3 门禁（本轮亲跑，全绿零回归）
- `npm run build` exit 0；`git diff --check` 干净。
- 全量 `npx vitest run` = **70 files / 573 passed / 0 failed / 0 skipped（58.8s）**（较 commit 7902059 的 562 多 11 = 本轮新增测试；含视觉基线 9/9、`slidev-bridge` 5/5 真 build、`annotate-browser` 3/3 真框选、`e2e-engine` PDF 真跑 xelatex）。
- `ReadLints` 四个改动源文件 + 两个测试文件 0 error。

#### 11.2.4 真机 3905 复验（PolarManager 固定端口，治本再验）
- 经 PolarProcess `POST /api/services/autooffice-agent-preview/restart` → **新 PID 31057、端口仍固定 3905**、restart_count=0、3s 内 health ok（再次实证 #1 端口不漂移）；新 dist 上线。
- 真 HTTP 铁证（`.planning/ide-engine/deck-text-live-proof.mjs`，gitignore）：对 live「自主 AI Agent 研究（Slidev）」框选 `slide-1-sub` 说「把全部『智能』统一改成『AO替换验证ZZ』」→ 响应 `patchSummary=deckTextReplace("智能"→"AO替换验证ZZ"; hits=8)`、源中 sentinel 出现 **8×**（框在第 1 页、泛化遍及全册），源中另 1 处「智能」在属性/frontmatter 内**正确未改**（证明保护）。
- **撤销归位**：`POST /undo` → live head 回到干净蓝色基线 `rev_11f8b7d2`（`insert image slide-1-img-720f3e`）= 用户既定基线；复查 head slides.md **无 sentinel、智能 9× 原样**、lastGood=同一版。我的验证 edit `rev_a8eb18ab` 为已撤销的 redo-orphan（非 head），与历史中既有的已撤销测试残留 recolor 同性质、对用户无影响（无删版 API，符合既有约定）。
- 稳定入口仍 **http://127.0.0.1:3905/aoide/**（刷新即得新 dist，可在浏览器里框选说「把全部 X 统一改成 Y」整册改名）。

#### 11.2.5 四项收口后的懂 PPT 泛化能力（现状）
| 轴 | 指令示例 | 作用域 | 实现 |
|---|---|---|---|
| 颜色/配色 | 「整体换暖色调」 | 整册 | `deckRecolorEdit`（GLM 选色 + 重涂色卡 + frontmatter accent） |
| 文字·术语统一 | 「把全部『AI』统一改成『人工智能』」 | 整册 | `deckTextEdit`（`deckTextReplace` 确定性替换，**新增**） |
| 文字·单句改写 | 「这句太啰嗦，精炼一点」 | 仅此处 | `llmRewriteText`（GLM 改写选中节点） |
| 插图 | 「这张换暖色」 | 整册配色 / 单图换色 | `deckRecolorEdit` / `llmImageEdit` |

## 12. 第八轮：用户 4 项追加（接力续接 agent-1-8d7d7ea9）
> 用户点选 4 项：① 浏览器里实测「把全部 X 统一改成 Y」；② 术语统一时**同步插图 alt 文案**；③ 做「多处 **LLM 语义统一**」增强；④ 把 commit `e9418c6` 推送远端。全程实时写本文件。

### 12.1 入场 recon
- 门禁复验：`git diff --check` 干净、上一轮 commit `e9418c6` 在册（HEAD）。
- **④ 推送远端 = 受阻（设计使然，非故障）**：`git remote -v` 显示 origin 的 **push URL 被刻意禁用** → `DISABLED-protect-private-history-from-peer-sync`（保护私有历史不被 peer 同步）；`main...origin/main [ahead 714, behind 5]`。→ 无法直接 push，且不擅自改 git remote/config、不 force。已回报用户请其决定（重开 push URL / 换远端 / 开 PR）。

### 12.2 执行进度（实时）
- [x] ② `deckTextReplace` 增 `syncAltLabels` 选项：术语统一时同步 `<img alt>`（绝不碰 data-ao-id/src）+ 单测；`deckTextEdit` 开启。
- [x] ③ 文字轴 deck **语义统一**：`hasGlobal && 无可解析 A→B` → GLM 多节点一致改写（`llmUnifyDeckText` + `deckSemanticEdit`）+ 单测；分类器 + 收集器纯函数可测。
- [x] 全绿门禁（无 LLM_EDIT 时套件零 GLM 调用）+ 真机 3905 复验（含 GLM）。
- [x] ① 浏览器 Playwright 实测整册改名 + 逐步截图亲看。
- [x] 修真机暴露的真 bug：measure 模式边缘组件（封面标题 y=-0.025）框选提交 500 → createAnnotation 服务端钳 rectNorm 到 [0,1] + 回归单测。

### 12.3 受 git 跟踪改动（全绿）
- `src/engine/slidev/edit.ts`：`deckTextReplace` 增 `opts.syncAltLabels`（术语统一时同步 `<img alt>` 文案，仍绝不碰 data-ao-id/src/其它属性）；新增纯函数 `collectDeckTextNodes(md)`（收集整册可编辑文字叶子节点：标题/副标题/正文/项目符，排除图片与容器）。经 `slidev/index.ts` 导出。
- `src/engine/llm-edit.ts`：新增 `llmUnifyDeckText(nodes, instruction)`——把整册所有文字节点（id+原文）交 GLM，返回 `{edits:[{id,text}]}` 一致改写映射（宽松 JSON 解析、未知 id 丢弃、失败降级空 map）。
- `src/engine/skills.ts`：文字轴 deck 分支扩展——`hasGlobal && 可解析 A→B` → 确定性整册改名（`textReplace`）；`hasGlobal && 无 A→B` → **整册语义统一**（scope=deck、axis=text、无 textReplace）；无全局词仍 local。
- `src/engine/service.ts`：① `createAnnotation` 起始钳 `rectNorm` 到 [0,1]（修边缘组件 500）；② `deckTextEdit` 传 `syncAltLabels:true`；③ 新增 `deckSemantic` 路由 + 私有 `deckSemanticEdit`（`collectDeckTextNodes`→`llmUnifyDeckText`→多节点 replaceText `EditIntent(scope:'multi')`→`applySlidevEditIntent` 作用域/连带校验→提交，`patchSummary=deckSemanticUnify(nodes=N)`，可撤销；GLM 无改动则不空提交）。
- 测试：`deck-theme.test.ts` +2（`syncAltLabels` 同步 alt 且不碰 id/src；`collectDeckTextNodes` 只收叶子文字节点）；`skills.test.ts` 改 1（全局无 A→B 现判 deck 语义）；`e2e-engine.test.ts` +1（越界 rect 钳位不再 500、仍解析成功）。

### 12.4 门禁 + 真机 3905 复验
- `npm run build` exit 0；`git diff --check` 干净；`ReadLints` 改动文件 0 error。
- 全量 `npx vitest run` = **70 files / 576 passed / 0 failed / 0 skipped**（无 `AUTOOFFICE_LLM_EDIT` 时套件零 GLM 调用；deckSemantic 路由仅在真机开关下触发）。
- 真机 3905（PolarManager 固定端口，restart 后仍 3905、health ok，新 dist 上线）：
  - **② alt 同步**：`deck-text2-live-proof.mjs` 框选说「把全部『智能』统一改成…」→ `deckTextReplace(…; hits=9)`（较上轮 8 多 1 = 图片 alt「…智能…」同步），`<img alt>` 已更新、data-ao-id/src 全保留；撤销归位。
  - **③ 语义统一**：框选说「把整册的语气和表述统一得更专业、更一致」→ 真 GLM 返回 `deckSemanticUnify(nodes=21)`，逐条 before/after 见术语/语气一致化改写（含义与角色保持）；撤销归位、head 回蓝色基线、无残留。
  - **① 浏览器实测**：`deck-rename-browser-demo.mjs` 驱动真实 /aoide/ App——选 Slidev 稿 → 点「框选修改」→ 点液态玻璃高亮热区选中 → 输入「把全部『研究』统一改成『探索』」→ 提交 → 整册重渲染（封面「研究现状与前沿·研究综述」→「探索现状与前沿·探索综述」，亲看 04-after 截图确认）→ 撤销恢复。5 张截图 + 自包含画廊 `agent-shots/rename-browser/rename-browser.html`。

### 12.5 ④ 推送远端 —— 受阻（需用户决定）
- origin 的 **push URL 被刻意禁用**（`DISABLED-protect-private-history-from-peer-sync`）、`main` ahead 714 / behind 5。不擅自改 remote/config、不 force push main。commit 安全落在本地。请用户决定：重开 push URL / 换个远端 / 或改为开 PR。

### 12.6 用户选 C（推新分支 / 开 PR）+ 浏览器再试语义统一 —— 已完成
- **recon**：SSH auth 正常（`ls-remote` 可读写 key）；远端满是 `agent/solo-web-*/<topic>` 分支 = 本仓 agent 工作的既定约定（main 受保护、agent 活推到 agent 分支）。本地 `main` 与 `origin/main` **无共同祖先**（origin/main 更旧、无引擎），故新分支必带完整本地史——与既有 agent 分支同性质。
- **C 落地**：`git push <显式URL> HEAD:refs/heads/agent/solo-web-8d7d7ea9/deck-wide-generalization`（不改 remote/config、不碰 main、非 force）→ 新分支已上（6.5s，共享既有对象）。PR-to-`main` 被 GitHub 拒（无共同历史）；查得 HEAD **恰好领先 `line/private-mac` 4 个提交（behind 0）= 有效私有主线**，遂对其开 PR：**https://github.com/beichenO2/AutoOffice/pull/1**（干净 4 提交：`7902059` IDE 基座 + `e9418c6`/`31ccfb6e` 两个 deck-wide + `e4c690b` docs）。
- **③ 浏览器再试整册语义统一**（`deck-semantic-browser-demo.mjs`，真实 /aoide/ App）：选 Slidev 稿 → 框选修改 → 点热区选中 → 输入没有 A→B 的整册意见「把整册的语气和表述统一得更专业、更一致」→ 提交（GLM 对该「统一」措辞约 50% 会判无需改，加了提交重试）→ **`deckSemanticUnify`、整册 16 个节点重渲染**（封面副标题「从大模型到会规划…」→「从大模型到具备规划、工具调用与协作能力的智能体」，亲看 04-after 截图确认）→ 撤销归位。5 张截图 + 画廊 `agent-shots/semantic-browser/semantic-browser.html`。
- 真机 3905 期间所有验证 edit 均已撤销，live head 稳定回到蓝色基线 `rev_11f8b7d2`。

### 12.7 语义统一对模糊指令更稳定触发 + docs 推进 PR + 画廊
- **改进（`llm-edit.ts llmUnifyDeckText`）**：① 强化提示词——要求 GLM 对可提升一致性/专业度的节点务必给出改写、不得因"已经不错"整体不改；② parse 侧过滤"改后=原文"的空改；③ **一次强制重试**：首轮 0 改动时以更强指令 + 稍高温度再问一次。门控不变（无 `AUTOOFFICE_LLM_EDIT` 时套件零 GLM 调用），全量仍 **576 绿**、build/lint 干净。
- **触发率实测**（`semantic-trigger-test.mjs`，真机 3905，故意用很模糊的「整体再统一一下，更专业一致些」）：**5/5 均 COMMIT**（deckSemanticUnify nodes=18~26），较改进前 ~50% 显著提升；每次都撤销、live head 归位基线。
- **docs 推进 PR**：把会话记录 + 本改进一并提交并推到 PR 分支 `agent/solo-web-8d7d7ea9/deck-wide-generalization`（PR #1 随之更新）。
- **画廊**：已在浏览器打开 `agent-shots/semantic-browser/semantic-browser.html`（16 节点 before/after 差异表 + 5 步截图）与 `agent-shots/rename-browser/rename-browser.html`（研究→探索 整册改名）。

### 12.8 多指令触发率复测 + 画廊入 PR + 语义单测（mock + 真实 GLM）
- **多条模糊指令触发率**（`semantic-trigger-multi.mjs`，真机 3905，每条 ×2）：**8/8 全部 COMMIT**——「整体再统一一下」2/2(nodes 24,31)、「把整册说得更专业点」2/2(24,23)、「风格统一一下，更一致」2/2(16,22)、「让全篇读起来更顺、更一致」2/2(13,11)；live head 每次归位基线。→ 改进后模糊指令触发率≈100%（改进前 ~50%）。
- **画廊入 PR（作为文件附件）**：`docs/demos/deck-wide/`（10 张真机截图 + `README.md` 画廊，GitHub 上直接渲染），commit `c57eb51` 已推入 PR 分支 → PR #1 里可内联查看两条 deck-wide 流程。
- **语义统一单测**（新增 2 文件）：
  - `tests/engine/deck-semantic.test.ts`（**mock GLM**，`vi.mock` llm-proxy，确定性、进套件）：`llmUnifyDeckText` 保留真改写 / 丢弃"改后=原文"空改 / 坏 JSON 优雅空 / **首轮空→强制重试一次**；`service.createAnnotation` 模糊整册指令 → 路由 `deckSemanticUnify` 落库并应用 mock 改写、可撤销；GLM 无改动则不空提交（5 例）。
  - `tests/engine/deck-semantic-live.test.ts`（**真实 GLM**，`checkLlmProxyHealth` 探活，proxy 不可用自动 `ctx.skip()`）：真调 GLM，模糊整册指令须返回>0 个真实（非 no-op、限已知节点）改写（1 例，本机 proxy 在→真跑通过 4.7s）。
- **门禁**：`git diff --check` 干净；全量 `npx vitest run` = **72 files / 582 passed / 0 failed**（较 576 多 6 = mock 5 + 真实 GLM 1；真实 GLM 用例在本机 proxy 可用时真跑、离线自动跳过，套件默认可离线全绿）。

## 13. 第九轮：封装成应用 + 5 主题调研做 PPT（用户）
> 需求：把 AutoOffice 封装成应用；并多做测试——上网找 5 个不同主题，分别调研 + 各生成一份 PPT。实时写本文件。

### 13.1 封装成应用（主题 → PPT 的应用化路径）
> 现有 `serve` 只起 IDE，`generate`/`batch` 走的是「结构化 JSON→报告」的旧管线；引擎的 `postRequirement` 走确定性稀疏 3 页。缺一条「给主题就产出丰富 PPT」的应用能力。本轮补上：
- `src/engine/llm-generate.ts`（新）：`llmGenerateDeckSpec(topic, research, {slides})`——主题+调研资料 → GLM 产结构化 DeckSpec（封面 + 每页小标题 + 3~5 要点/一段论述），宽松 JSON 解析、失败返回 null；`fallbackDeckSpec(topic, research)`——离线/无 GLM 时按调研行确定性拼多页（比旧 3 页富）。
- `src/engine/service.ts` `createDeckFromTopic(name, topic, research?)`：LLM_EDIT 开→GLM 富deck、否则 fallback；`renderSlidevSource`→`previewHtmlFromSource`+`buildSlidevDeckBoxes`→提交 `origin:generation` revision（在 /aoide/ 里即可框选编辑）。返回 `{project,revision,usedLlm,slides}`。
- `src/cli.ts` 新命令 **`generate-deck`**：`--topic`（必）`--research <file>` `--name` `--home <dir>` `--out <dir>` `--export pdf|pptx|html`。这就是「应用」的无头入口：一条命令把主题变成可编辑 deck + 预览 HTML + 导出文件；配 `serve` 的 /aoide/ 即完整应用。
- 单测 `tests/engine/deck-generate.test.ts`（5 例，mock GLM）：`llmGenerateDeckSpec` 映射封面/要点/论述、坏输出返回 null；`fallbackDeckSpec` 多页；`createDeckFromTopic` GLM 富deck 落库+可框选、无 LLM_EDIT 走 fallback。

### 13.2 5 主题调研 + 各做 PPT（真机真 GLM 铁证）
- 上网（WebSearch）研究 5 个**不同领域**主题并沉淀要点：① 固态电池（能源/EV）② GLP-1 减重药（医药）③ 韦布望远镜发现（空间科学）④ 欧盟 AI 法案（政策/法律）⑤ 人形机器人（机器人/产业）。资料存 `.planning/ide-engine/app-test/topics.mjs`。
- 经应用路径（真 GLM）各生成一份 PPT（`run.mjs`）：**5/5 全部用 GLM**，5~6 页、每份 10~13 条**有信息量且忠于调研**的要点（如「MoM-z14 红移 z=14.44」「OASIS 4 减重 16.6%~17%」「10^25 FLOPs」「Optimus 内部 300~500 台」）。逐页 Playwright 截图亲看：封面深色品牌页、内容页眉标+下划线+菱形项目符+页码，专业清晰（见 3-韦布 content、2-GLP-1 cover 截图）。
- **CLI 冒烟**：`node dist/cli.js generate-deck --topic 固态电池… --export pptx` → 6 页 llm=true、写预览 HTML + **导出 PPTX 808KB**，证明打包命令 + 导出端到端可用。
- 画廊：`.planning/ide-engine/app-test/out/app-5decks.html`（本地）；并作为 PR 附件落 `docs/demos/app-5decks/`（10 图 + README）。

### 13.3 门禁 + PR
- `npm run build` exit 0；`git diff --check` 干净；全量 `npx vitest run` = **73 files / 587 passed / 0 failed**（较 582 多 5 = deck-generate）。
- 提交源码 + 单测 + 画廊 + 文档并推 PR 分支。稳定 IDE 入口仍 http://127.0.0.1:3905/aoide/。

## 14. 第十轮：用户 5 项新需求（接力续接 agent-1-7f304902，接 HB5T/8d7d7ea9）
> 需求原文（转移附言）：①给 /aoide/ 加「输入主题一键生成」按钮；②把应用打包成桌面 App；③再测几个更难的专业主题；④要求可根据**大纲+指导内容+选好的插图**来生成 PPT（没那么自由、更严谨——验证数据严谨、更详细介绍、适配 LaTeX 复杂公式合理插入）；⑤更多门禁测试（AI/数学/医学方向；文字多/排版多/公式多/动画多，分别独立），帮忙生成测试情景与测试要求。

### 14.0 入场 recon（不沿用旧结论）
- `git status`：最后 commit 仍是第 9 轮 `3f200fc`；工作树有**未提交 WIP**（前一手第 10 轮起了个头但未记录未提交）：`src/cli.ts`（generate-deck 加 `--outline/--guidance/--image/--formulas`）、`src/engine/llm-generate.ts`（`llmGenerateDeckSpec` 加 outline/guidance/allowFormulas + `insertImagesIntoSpec`）、`src/engine/html/generate.ts` + `slidev/generate.ts`（KaTeX→MathML `renderFormulaMathml` + formula 元素）、`src/engine/service.ts`（`createDeckFromTopic` 加 outline/guidance/images/allowFormulas）、`tests/engine/deck-generate.test.ts`（+4 guided/formula）、`docs/demos/test-scenarios.md` + `docs/demos/gate-scenarios/`（S1–S7 情景 + S1/S3/S5 画廊）。
- 绿色基线：`npm run build` exit 0；全量 `npx vitest run` = **73 files / 591 passed / 0 failed**（含 WIP 的 +4）。→ 引擎/CLI/公式脚手架已在，**缺**：#1 HTTP 路由+前端按钮、#4 的「数据严谨校验」机制、#5 自动化门禁测试(vitest)、#2 桌面 App、S7 动画。
- 3905 托管服务健康（PolarProcess `127.0.0.1:11055`；预览端口固定 3905）。

### 14.1 #1「输入主题一键生成」按钮 + #4 UI 暴露 —— 已完成（真机+浏览器实测）
- **后端路由**（`src/engine/routes.ts`）：新增 `POST /api/engine/decks` → `svc.createDeckFromTopic`，body 支持 `{topic(必), name?, research?, outline?, guidance?, formulas?, slides?, images?}`；topic 空 → 400 `invalid_topic`；images 过滤空 src、截断 12；slides 钳 [4,12]。返回 `{project,revision,usedLlm,slides,grounding}`。
- **前端**：
  - `public/aoide/index.html`：左栏头部加 `#generate-deck`「一键生成」主按钮 + 空态 `#empty-generate` 主按钮 + `data-topic` 主题 chips；新增 `#generate-modal` 模态框（主题必填 + `<details>` 定制：大纲/权威资料/插图链接/「允许 LaTeX 公式」勾选/页数）；新增 `#i-sparkles`/`#i-sigma` 图标。
  - `public/aoide/css/app.css`：`.ao-modal*` 液态玻璃模态框 + `.ao-btn--lg` + `.ao-checkbox` + `.ao-input--num`（避开已占用的 `.ao-check`）。
  - `public/aoide/js/ui/generate.js`（新）：模态框控制器（开/关/校验/busy；images textarea 解析「N: url」定位到第 N 页）。
  - `public/aoide/js/main.js`：`generateDeck(payload)` → `api.generateDeck` → 刷新项目列表并打开新 deck；按 `grounding` 弹 #4 反馈 toast。`api.js` 加 `generateDeck`。空态 chips 由 `data-example`（预填输入框）改为 `data-topic`（开模态框）。
- **浏览器实测**（Playwright `generate-button-demo.mjs`，真实 :3905 App，真 GLM）：点「一键生成」→ 模态框弹出（截图）→ 填数学主题「傅里叶变换与卷积定理」+ 展开定制填大纲/指导 + 勾选公式 → 提交 → **按 4 页大纲生成 4 页可编辑 deck，渲染 6 个 MathML 公式 + 6 个可框选 formula 节点**；封面/公式页逐页截图亲看；画廊 `agent-shots/generate-button/generate-button.html`。公式页视觉铁证：\(F(\omega)=\int_{-\infty}^{\infty}f(t)e^{-i\omega t}\,dt\) 正确排版（积分号/上下限/指数/微分俱全）。

### 14.2 #4 数据严谨校验（「验证具体数据是否严谨」）—— 已完成
- `src/engine/llm-generate.ts` 新增纯函数 `extractSpecificNumbers`（提取百分比/小数/≥10 的多位数，跳过裸单个数字 0–9 避免误报）+ `verifyDeckGrounding(spec, sources)`（扫描每个文本节点的具体数字是否能在 guidance+research 中找到出处；**公式/图片豁免**——其数字是记号/资产非事实主张）；返回 `{checked, grounded, flagged:[{slide,text,numbers}]}`，纯确定性可单测、advisory 不阻断。
- `src/engine/service.ts` `createDeckFromTopic` 计算 `grounding` 并入返回；前端 `generateDeck` 据此弹 toast：有未溯源数字 → 红字「N/M 处有出处，X 处未在资料中找到，请核对第 …页」；全溯源 → 绿字「N 处具体数字均能找到出处」。
- 真机复验：傅里叶 deck grounding checked=0（该主题公式为主、少具体数字，符合预期）；单测覆盖「16.6% 溯源=grounded、99999 杜撰=flagged、公式豁免」。

### 14.3 #5 自动化门禁测试 + 情景 —— 已完成
- **情景文档**（承接 WIP `docs/demos/test-scenarios.md`）：S1–S7 覆盖 AI/数学/医学 × 图文排版/文字多/公式多/复杂公式/文字详实/药代模型/动画；通用验收 G1 数据严谨 / G2 结构完整 / G3 可编辑 / G4 渲染。
- **自动化门禁测试**（新 `tests/engine/gate-scenarios.test.ts`，mock GLM 确定性、离线安全，8 例）：S1 插图定点插入指定页且可框选；S2 遵循大纲/页数≥7/要点密度≥24 无空页；S3 每内容页 LaTeX→MathML 且公式可框选；S4 复杂公式（∫上下限/指数）渲染为结构化 `<msubsup>/<msup>`；S5 忠于 guidance 数字判 grounded、杜撰数字被 flagged；S6 药代/半衰期公式渲染含 `<mfrac>`；+ `extractSpecificNumbers`/`verifyDeckGrounding` 单测。
- **HTTP 路由门禁**（`tests/server.test.ts` +2）：`POST /api/engine/decks` 主题→201（含 grounding）、缺 topic→400；转发服务加 `AUTOOFFICE_ENGINE_HOME`(temp)+`BOXMAP=estimate` 隔离、离线、无 Chromium。

### 14.4 #3 更难专业主题（真机真 GLM）—— 傅里叶已验，续测中
- 傅里叶变换与卷积定理（数学·复杂公式）：真机 `/decks` 路由 201、usedLlm=true、4 页匹配大纲、2×MathML；浏览器按钮流再验 6×MathML。
- 后续可续：卷积定理/药代动力学/量子纠错等（公式+医学方向），逐页截图入画廊。

### 14.5 门禁（阶段一亲跑，全绿零回归）
- `npm run build` exit 0；`ReadLints` 改动源文件 0 error；全量 `npx vitest run` = **74 files / 601 passed**（较 591 多 10）。

### 14.6 用户拍板：全做 + 都要 private 仓库
> 用户回复：#2 打包 Electron 桌面 App 并提交推 PR；再测更难主题（医学药代 + AI 大模型安全）；做 S7 动画（Slidev v-click/transition）；「该提交提交，但都要 private 仓库」。
> recon：`gh repo view beichenO2/AutoOffice` → **isPrivate:true / PRIVATE**（既满足「私有仓库」），PR #1 就在其上；push URL 仍被禁用（`DISABLED-…`），沿用显式 URL 推送、不改 remote/config、不碰 main。

### 14.7 S7 动画（Slidev v-click/transition）—— 已完成
- **低风险设计**（不引入每页 frontmatter，避免破坏 `splitSlidevPages`/preview/boxmap）：deck 级 `transition:`（写进唯一的 header frontmatter）+ 每个可揭示正文元素（bullet/paragraph/formula）加 `v-click` 属性。静态沙箱预览里 `v-click`/`transition` 为惰性（元素全显、盒图/编辑契约不变），slidev 导出/dev 则为真过渡 + 逐步揭示。封面/标题永不揭示。
- 代码：`html/generate.ts` 加 `SlideSpec.reveal?` + `DeckSpec.transition?`；`slidev/generate.ts` `renderElement(reveal)` 给可揭示元素加 `v-click`、`renderSlidesMd` 按 `deck.transition` 写 headmatter；`llm-generate.ts` `applyDeckAnimation(spec)`（纯函数：标题页外 reveal=true + 默认 slide-left）；`service.createDeckFromTopic({animate})`；CLI `--animate`；route `animate`；前端模态框「加入动画」勾选 + `generate.js` 收集。
- 测试：`gate-scenarios.test.ts` S7（animate → headmatter `transition:` + 内容页 `<li v-click>` ≥3 + paragraph v-click + 封面副标题无 v-click + splitSlidevPages 仍分 3 页）。e2e-engine 9 例仍绿（v-click/transition 不破坏框选-撤销）。

### 14.8 #4 公式增强：正文行内 `$…$` → MathML —— 已完成（真机暴露的真缺口）
- **缺口**：真机药代 deck 里 GLM 把公式写进 paragraph 正文散文（裸 LaTeX `C(t)=(D/V)e^{-k_e t}`，显示为源码，未渲染），只有走 `formulas` 字段的才渲染为 MathML。
- **修复**：`html/generate.ts` 新增 `renderInlineText(raw)`——把文本里的 `$…$` 段渲染为行内 MathML、其余转义；无 `$` 时走原 `escapeHtml`（行为不变）。`heading/subheading/paragraph/note/bullet` 渲染改用它（slidev + html 两条管线）。并强化 GLM 提示词：独立公式一律进 `formulas` 字段、正文如需引用公式必须用 `$…$` 包裹、不得写裸 LaTeX。
- 测试：`gate-scenarios.test.ts` S3b（bullet/paragraph 里的 `$…$` 渲染为 `<math` 且不泄漏裸 LaTeX）+ `renderInlineText` 单测 2 例（`$…$`→MathML；无 `$` 精确等于 escapeHtml）。视觉基线 9/9 不变（无 `$` 文本行为不变）。

### 14.9 #3 更难专业主题（真机真 GLM）—— 已完成
- **傅里叶变换与卷积定理**（数学·复杂公式）：`/decks` 201、4 页匹配大纲、6×MathML（含 \(F(\omega)=\int_{-\infty}^{\infty}f(t)e^{-i\omega t}\,dt\)）；浏览器按钮流亲验。
- **一室药代动力学模型**（医学·公式+动画）：`/decks` 201、usedLlm=true、5 页匹配大纲、5×MathML、26×v-click；公式页 \(C(t)=(D/V)e^{-k_e t}\) 截图亲看。
- **大模型安全与对齐**（AI·文字多+动画）：`/decks` 201、5 页匹配大纲、标题逐一对应大纲、2×MathML、19×v-click；**数据严谨校验 checked=3 全部溯源 flagged=0**（10^25 FLOPs、2026-08-02 等来自 guidance）。
- 画廊：`.planning/ide-engine/agent-shots/hard-topics/hard-topics.html`（药代+安全 封面/内容页）、`agent-shots/generate-button/`（按钮流+傅里叶公式页）。

### 14.10 #2 Electron 桌面 App —— 已完成（server-boot 冒烟通过）
- `electron/main.cjs`：选空闲端口 → 以 `ELECTRON_RUN_AS_NODE` 拉起 `server-boot.mjs`（即 `startServer(port)`，绕开 Polar 治理、仅 127.0.0.1）→ 轮询 `/health` → 原生窗口加载 `/aoide/`；mac 保活、退出 kill 子进程；外链走系统浏览器。`server-boot.mjs`（ESM 引导）、`preload.cjs`（contextIsolation + `window.autooffice.desktop` 标记）、`electron/README.md`。
- `package.json`：加 `main: electron/main.cjs`、脚本 `app`/`app:pack`/`app:dist`、electron-builder `build` 配置（mac dmg / win nsis / linux AppImage；`asarUnpack` 放行 playwright-chromium）；devDeps 加 `electron@^43` + `electron-builder@^26`（已 `npm install`，electron 二进制 v43.2.0 就绪）。`.gitignore` 加 `release/`。
- **冒烟验证**（纯 Node 跑 server-boot，无 GUI）：`startServer` 起于随机端口 → `/health`=200、`/aoide/`=200、`POST /api/engine/decks`=201（slides=2 fallback）。→ 桌面 App 的服务启动路径端到端可用；GUI 窗口逻辑已 `node --check` 通过（GUI 需显示器，无法在无头环境实跑）。server.test.ts 亦证「fork node→serve→/aoide/+/api/engine/decks」同路径可用。

### 14.11 门禁（阶段二亲跑，全绿零回归）
- `npm run build` exit 0；全量 `npx vitest run` = **74 files / 605 passed / 0 failed / 0 skipped（61s）**（较 601 多 4 = S7 1 + S3b 1 + renderInlineText 2）。
- 稳定入口 http://127.0.0.1:3905/aoide/（PolarProcess restart 后新 PID、端口仍 3905、新 dist 上线）。

### 14.12 提交 + 推私有 PR —— 已完成
- **commit `da1c899`** `feat(app+ide): topic→deck UI, guided/animated generation, data-rigor check, gate tests, Electron desktop app`（32 文件 +4563；精确暂存，刻意排除噪声 `lobster-events.jsonl` + cron `reports/*`）。
- 显式 URL 推送（不改 remote/config、不碰 main、非 force）：`git push git@github.com:beichenO2/AutoOffice.git HEAD:refs/heads/agent/solo-web-8d7d7ea9/deck-wide-generalization` → `3f200fc..da1c899` 快进。
- **私有仓库确认**：`gh repo view beichenO2/AutoOffice` → isPrivate:true / PRIVATE；**PR #1 现 10 个提交**，最新即本轮：https://github.com/beichenO2/AutoOffice/pull/1
- 画廊已入 PR：`docs/demos/gate-scenarios/`（+ generate-button.html、hard-topics.html 自包含画廊）。

### 14.13 第十轮五项收口
| # | 需求 | 状态 | 关键实证 |
|---|---|---|---|
| 1 | /aoide/「输入主题一键生成」按钮 | ✅ | POST /decks 路由 + 模态框 + 浏览器实测；真机生成 4~5 页匹配大纲 |
| 2 | 打包桌面 App | ✅ | Electron（electron/ + electron-builder 配置）；server-boot 冒烟 health/aoide/decks 全过 |
| 3 | 更难专业主题 | ✅ | 傅里叶(数学)/一室药代(医学·公式)/大模型安全(AI·文字) 真机真 GLM + 画廊 |
| 4 | 大纲+指导+插图→PPT + LaTeX | ✅ | createDeckFromTopic 全参 + KaTeX→MathML + 行内 $…$ + verifyDeckGrounding 数据严谨校验 |
| 5 | 更多门禁测试 | ✅ | test-scenarios.md S1–S7 + gate-scenarios.test.ts(12) + server(2)；全量 605 绿 |
- 稳定入口 http://127.0.0.1:3905/aoide/（一键生成 + 大纲/指导/插图/公式/动画定制）；桌面版 `npm run app`。

## 15. 第十一轮：导出分步动画 + 出 dmg + 开画廊（接力续接 agent-1-092e2ec6，接 J3OI/7f304902）
> 用户请求原文（存档最后一条，23:39:16）：① 真机导出带分步动画的 PDF/PPTX（`--with-clicks`）；② 跑 `npm run app:dist` 真出一个 dmg 安装包；③ 浏览器里打开两个新画廊看效果。所有工作实时写本文件。

### 15.0 入场 recon（不沿用旧结论）
- `git status`：最后 commit 仍 `da1c899`（第十轮）。工作树有**上一手 J3OI 未提交的 WIP**（`--with-clicks` 导出功能，已写完代码未验证未提交）：`src/engine/routes.ts`（export 路由加 `?clicks=1`）、`src/engine/service.ts`（`exportProject(..., opts.withClicks)` + 含 v-click 自动开启）、`src/engine/slidev/cli.ts`（`slidevExportPptx` 加 `withClicks` 参数）、`src/engine/slidev/generate.ts`（新增 `deckHasClicks(source)` 检测 `\bv-click\b`）、`src/engine/slidev/index.ts`（导出 `deckHasClicks`）、`tests/engine/gate-scenarios.test.ts`（S7 断言 deckHasClicks true + 新增 S7b 静态 deck 不触发）。
- 绿色基线：`npm run build` exit 0；工具链 Node v22.22.2 / slidev 52.15.2 / playwright-chromium 就绪 / pdfinfo+pdftoppm(poppler) 可用。
- 两个「新画廊」确认 = `docs/demos/gate-scenarios/generate-button.html`（4.2MB「输入主题一键生成 · 浏览器实测」）+ `hard-topics.html`（2.9MB「更难专业主题 · 药代 + 大模型安全」），均自包含内嵌 4 张 base64 截图。

### 15.1 #1 真机导出带分步动画（`--with-clicks`）—— 已完成（真机铁证）
- WIP 代码已把 `--with-clicks` 端到端接好，本轮**端到端真跑 + 亲验产物**证明有效。
- 驱动 `.planning/ide-engine/export-clicks-proof.mjs`（gitignore、真 slidev+chromium）：`applyDeckAnimation` 造确定性动画 deck（4 页：封面 + 核心挑战 3 要点 + 关键方法 1 段+3 要点 + 采样公式 1 公式+2 要点 = **10 个 v-click**、transition=slide-left），走 service-path helper 导出三次比对：
  - **PDF 不带 clicks（基线）= 4 页**（== 幻灯数）。
  - **PDF 带 `--with-clicks` = 14 页**（4 幻灯 + 10 揭示步骤，每步各成一页）→ 页数扩张铁证成立。
  - **PPTX 带 `--with-clicks` = 14 张**（合法 PK zip、638KB）→ 同样按步扩张。
  - 数学吻合：`4 slides + 10 v-click = 14`。耗时各 ~3–5s。
- 栅格化亲看（`agent-shots/export-clicks/withclicks-*.png` 14 张）：第 02 页「核心挑战」仅标题（动画初始态）→ 03 页 +第 1 条要点 → 05 页三条全出（逐条递增揭示）；第 12 页「采样公式」把复杂公式 \(x_{t-1}=\sqrt{\alpha_{t-1}}\hat{x}_0+\sqrt{1-\alpha_{t-1}}\epsilon_\theta(x_t,t)\) 渲染为 MathML、导出后完好。→ 分步揭示 + 转场 + 公式导出全部真机验证。
- 门禁：全量 `npx vitest run` = **74 files / 606 passed / 0 failed / 0 skipped（60s）**（较第十轮 605 多 1 = WIP 新增 S7b）；含真实 slidev build 5.7s、视觉基线 9/9、annotate-browser 3/3（PDF 51.4s 真跑 xelatex）、真 GLM 语义统一。

### 15.2 #3 浏览器打开两个新画廊 —— 已完成
- `open docs/demos/gate-scenarios/generate-button.html` + `hard-topics.html` → 已在默认浏览器打开两个自包含画廊看效果。

### 15.3 #2 跑 `npm run app:dist` 出 dmg —— 已完成（真 dmg，已校验可挂载）
- `rm -rf release && npm run app:dist`（= `npm run build && electron-builder`）：electron-builder 26.15.3、@electron/rebuild 完成、electron 43.2.0 下载解压 100%、**打包 `release/mac-arm64/AutoOffice.app`（693M，Info.plist=com.polarisor.autooffice、执行体 33K、app.asar 364M）完成**、代码签名因无 Developer ID 跳过（开发环境正常）、用默认 Electron 图标。
- **踩坑：并发 dmg 死锁**。发现工作树里有上一手 J3OI 于 23:43 起的 `npm run app:dist` **孤儿进程仍在跑**（PID 16552），我 23:48 又起了一个（PID 29043）——两个 electron-builder 并发写同一 `release/` 到最后 dmg 封装步骤**互相死锁**（均 0% CPU、无子进程、无产出）；`kill` 被 Polar 治理拦截（`⛔ 直接 kill 发进程信号`）无法终止，`/Volumes/` 无卡死挂载。
- **治法（绕开死锁、保证正确）**：electron-builder 的 **.app 已完整打包**（asar 校验：24918 条目，含 `dist/cli.js`/`public/aoide/index.html`/`electron/main.cjs`，写入是临时文件+原子重命名故 .app 是单个构建的完整产物，死锁仅在 dmg 封装阶段）→ 用可控的 **`hdiutil create`** 从这个完好 .app 亲手封 dmg：`ditto` 复制 app + `ln -s /Applications`（拖拽安装 UX）+ `hdiutil create -volname "AutoOffice 0.4.0" -format UDZO -fs HFS+`（唯一卷名避碰撞）。
- **产物 + 核验**：`release/AutoOffice-0.4.0-arm64.dmg` = **248M**（sha256 前缀 `2daa5bf6f4ebfb60`）；`hdiutil verify` = **checksum VALID**；`hdiutil attach` 挂载于 `/Volumes/AutoOffice 0.4.0`，内含 `AutoOffice.app`（执行体 + app.asar 364M 齐全、identifier 正确）+ `Applications` 软链，干净 detach。→ 真·可拖拽安装的 dmg 成立。
- 遗留：两个死锁的 electron-builder 孤儿进程（PID 16552/29043）因 `kill` 被治理拦无法回收，均 0% CPU 不占用、对用户无影响（同既有孤儿实例约定，可由用户手动清理）。清理了 693M 冗余 `release/dmg-stage`。

### 15.4 门禁 + 三项收口
- `npm run build` exit 0；`git diff --check` 干净；全量 `npx vitest run` = **74 files / 606 passed / 0 failed / 0 skipped（60s）**（较第十轮 605 多 1 = WIP 的 S7b）。
- WIP（`--with-clicks` 导出功能，J3OI 未提交）本轮已**验证全绿 + 真机导出铁证**，但**未提交**（本轮用户请求未要求提交；如需可 commit + 推 PR #1）。

| # | 请求 | 状态 | 关键实证 |
|---|---|---|---|
| 1 | 真机导出带分步动画 PDF/PPTX（--with-clicks） | ✅ | PDF 4→**14 页**、PPTX **14 张**（4 幻灯+10 v-click），栅格化亲看逐步揭示+公式 MathML；export-clicks-proof.mjs |
| 2 | `npm run app:dist` 出 dmg | ✅ | `release/AutoOffice-0.4.0-arm64.dmg` 248M，hdiutil verify VALID + 挂载核验 app 完整（绕开并发死锁用 hdiutil 亲封） |
| 3 | 浏览器打开两个新画廊 | ✅ | `open` generate-button.html（4.2MB）+ hard-topics.html（2.9MB） |

